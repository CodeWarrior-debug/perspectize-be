---
phase: 03.2-activity-page-beta-quality
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/components/Header.svelte
  - frontend/src/routes/+page.svelte
  - frontend/src/lib/components/ActivityTable.svelte
  - frontend/src/app.css
  - frontend/tests/components/Header.test.ts
autonomous: true

must_haves:
  truths:
    - "Header has navy (#1a365d) background with white text and white icons"
    - "AG Grid is wrapped in a card container with border, rounded corners, and shadow"
    - "AG Grid has no console warnings about missing --ag-row-height or --ag-header-height"
  artifacts:
    - path: "frontend/src/lib/components/Header.svelte"
      provides: "Navy header with bg-primary text-primary-foreground"
      contains: "bg-primary"
    - path: "frontend/src/routes/+page.svelte"
      provides: "Card wrapper around ActivityTable"
      contains: "rounded-lg"
    - path: "frontend/src/lib/components/ActivityTable.svelte"
      provides: "AG Grid theme with explicit rowHeight and headerHeight"
      contains: "rowHeight"
  key_links:
    - from: "Header.svelte"
      to: "app.css --color-primary"
      via: "bg-primary class"
      pattern: "bg-primary"
---

<objective>
Fix header styling to navy background and wrap AG Grid in a card container.

Purpose: Address D-01 (white header should be navy), D-02 (no card around grid), and D-06 (AG Grid theme param warnings).
Output: Visually correct header and grid container matching design spec.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/lib/components/Header.svelte
@frontend/src/routes/+page.svelte
@frontend/src/lib/components/ActivityTable.svelte
@frontend/src/app.css
@frontend/tests/components/Header.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navy header + card container + AG Grid theme params</name>
  <files>
    frontend/src/lib/components/Header.svelte
    frontend/src/routes/+page.svelte
    frontend/src/lib/components/ActivityTable.svelte
    frontend/src/app.css
  </files>
  <action>
**Header.svelte (D-01):**
Replace `bg-white` with `bg-primary text-primary-foreground` on the header element.
Update the logo link: change `text-foreground hover:text-primary` to `text-primary-foreground hover:text-primary-foreground/80`.
The UserSelector and AddVideoPopover buttons inside the header need to be visible on navy. Add a wrapper div with appropriate classes so buttons/select use light-on-dark styling. The UserSelector native select will need `bg-primary-foreground/10 text-primary-foreground border-primary-foreground/20` or similar to be readable on navy. The AddVideoPopover trigger button already uses `buttonVariants()` which defaults to primary variant - on a navy header this will be hard to see. Change its triggerVariant to "outline" and ensure the outline variant looks good on navy by adding header-specific overrides if needed, OR use `secondary` variant for visibility.

Simpler approach for header child elements: wrap the right-side div in a class that sets text color for all descendants. Use CSS custom properties or Tailwind's group pattern. The key constraint is: logo text = white, user selector = readable on navy, buttons = readable on navy.

For the UserSelector native `<select>` element: it currently uses `bg-background` which is white. On navy header it will still look fine as a white dropdown on navy background. Leave it as-is for now (D-03 will replace it with shadcn Select in a later plan).

For AddVideoPopover and CreateUserPopover trigger buttons: these use FormPopover which renders with `buttonVariants()`. On a navy background, the default primary button (navy on navy) is invisible. Pass `triggerVariant="secondary"` to make them visible (light gray button on navy).

**+page.svelte (D-02):**
Wrap the ActivityTable div in a card container: `<div class="border rounded-lg shadow-sm overflow-hidden">`. The card should wrap the grid, not the page title. Apply the card wrapper around the `<ActivityTable />` component's container div.

**ActivityTable.svelte (D-06):**
The AG Grid theme already has `rowHeight: 44` and `headerHeight: 40` in `themeQuartz.withParams()`. Verify these are present. If there are CSS variable warnings, also add explicit CSS custom properties in the grid container:
```
style="--ag-row-height: 44px; --ag-header-height: 40px;"
```
on the grid wrapper div.

**app.css:**
Remove the old AG Grid pagination panel CSS overrides (lines 91-111) since we now use manual pagination controls, not AG Grid's built-in pagination. These orphaned styles serve no purpose.
  </action>
  <verify>
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run check` - zero type errors.
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run test:run` - all tests pass.
  </verify>
  <done>
    Header has bg-primary text-primary-foreground classes. AG Grid wrapped in border rounded-lg shadow-sm overflow-hidden. AG Grid theme params include rowHeight and headerHeight. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Header tests for navy styling</name>
  <files>
    frontend/tests/components/Header.test.ts
  </files>
  <action>
Update Header component tests to verify the navy styling:
- Test that the header element has `bg-primary` class
- Test that the header element has `text-primary-foreground` class
- Update any existing tests that assert `bg-white` to assert `bg-primary` instead
- Ensure all existing Header tests still pass (logo text, UserSelector presence, AddVideoPopover presence)
  </action>
  <verify>
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run test:run` - all tests pass.
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run test:coverage` - coverage thresholds met.
  </verify>
  <done>
    Header tests verify bg-primary and text-primary-foreground. All tests pass. Coverage thresholds met.
  </done>
</task>

</tasks>

<verification>
- `pnpm run check` passes with 0 errors
- `pnpm run test:run` passes all tests
- `pnpm run test:coverage` meets 80% stmts/lines/functions, 75% branches
- Header element has bg-primary text-primary-foreground
- ActivityTable is inside a div with border rounded-lg shadow-sm overflow-hidden
</verification>

<success_criteria>
- Header background is navy (#1a365d) with white text
- AG Grid has a card container with border and rounded corners
- No AG Grid theme variable warnings
- All tests pass with coverage thresholds met
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-activity-page-beta-quality/03.2-05-SUMMARY.md`
</output>
