---
phase: 03.2-activity-page-beta-quality
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/components/ActivityTable.svelte
  - frontend/src/lib/utils/formatting.ts
  - frontend/tests/unit/formatting.test.ts
autonomous: true

must_haves:
  truths:
    - "All sortable columns (Item, Views, Likes, Date) have floating filters enabled"
    - "Item column uses agTextColumnFilter"
    - "Views and Likes columns use agNumberColumnFilter"
    - "Date column uses agDateColumnFilter or agTextColumnFilter"
    - "YouTube icon is vertically and horizontally centered within its cell"
  artifacts:
    - path: "frontend/src/lib/components/ActivityTable.svelte"
      provides: "Column defs with filter types and floating filters"
      contains: "agTextColumnFilter"
    - path: "frontend/src/lib/utils/formatting.ts"
      provides: "typeCellRenderer with flexbox centering"
      contains: "items-center justify-center"
  key_links:
    - from: "ActivityTable.svelte columnDefs"
      to: "AG Grid filter model"
      via: "filter and floatingFilter properties"
      pattern: "filter:.*ag.*ColumnFilter"
---

<objective>
Enable proper filters on all sortable/filterable columns and fix YouTube icon centering.

Purpose: Address D-09 (most columns missing filters/sort) and D-10 (YouTube icon not centered in cell).
Output: All data columns have appropriate filter types; YouTube icon is perfectly centered.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/lib/components/ActivityTable.svelte
@frontend/src/lib/utils/formatting.ts
@frontend/tests/unit/formatting.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable column filters with correct types</name>
  <files>
    frontend/src/lib/components/ActivityTable.svelte
  </files>
  <action>
Update columnDefs in ActivityTable.svelte to add proper filter types and floating filters to ALL sortable/filterable columns:

**Item column (already has filter: true):**
- Change `filter: true` to `filter: 'agTextColumnFilter'` (explicit type)
- Already has `floatingFilter: true` - keep it

**Type column:**
- No filter needed (not sortable/filterable) - leave as-is

**Length column:**
- Not sortable in backend - leave as-is (no filter)

**Views column (already has floatingFilter: true):**
- Add explicit `filter: 'agNumberColumnFilter'`
- Keep `floatingFilter: true`

**Likes column (already has floatingFilter: true):**
- Add explicit `filter: 'agNumberColumnFilter'`
- Keep `floatingFilter: true`

**Date (publishDate) column (already has floatingFilter: true):**
- Add explicit `filter: 'agTextColumnFilter'` (text filter for formatted dates since our dates are formatted strings, not Date objects). Using agTextColumnFilter because our valueFormatter converts dates to locale strings. agDateColumnFilter would require raw Date objects which we don't provide.
- Keep `floatingFilter: true`

**Hidden columns that are sortable (createdAt, updatedAt):**
- Add `filter: 'agTextColumnFilter'` and `floatingFilter: true` to both

**Hidden columns that are not sortable (channel, tags, description):**
- Add `filter: 'agTextColumnFilter'` and `floatingFilter: true` to channel (useful for search)
- Tags and description: add `filter: 'agTextColumnFilter'` and `floatingFilter: true`

Also update the `defaultColDef` to NOT include filter: true globally - each column controls its own filter.

IMPORTANT: The `onFilterChanged` handler already collects all filter values into a single search string sent to the backend. This means AG Grid floating filters provide the UI, but the actual filtering is server-side via the GraphQL search parameter. This is correct behavior - the filters act as a frontend entry point for the backend search.
  </action>
  <verify>
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run check` - zero type errors.
  </verify>
  <done>
    All sortable columns have explicit filter types. All columns with data have floatingFilter: true.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix YouTube icon centering in type cell</name>
  <files>
    frontend/src/lib/utils/formatting.ts
    frontend/tests/unit/formatting.test.ts
  </files>
  <action>
**formatting.ts - typeCellRenderer (D-10):**
The current typeCellRenderer creates a container div with `flex items-center justify-center`. However, AG Grid cells have their own layout that may not respect the div's centering. The fix:

1. Add `h-full w-full` to the container div className so it fills the entire cell:
   `container.className = 'flex items-center justify-center h-full w-full';`

2. This ensures the flexbox centering works within the full cell dimensions.

**formatting.test.ts:**
Add or update tests for typeCellRenderer:
- Verify the container div has classes `flex items-center justify-center h-full w-full`
- Verify the SVG element exists inside the container
- Verify the SVG has the correct YouTube red fill (#FF0000)

Run all tests to ensure nothing breaks.
  </action>
  <verify>
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run test:run` - all tests pass.
    Run `cd /Users/jamesjordan/GitHub/perspectize/frontend && pnpm run test:coverage` - coverage thresholds met.
  </verify>
  <done>
    YouTube icon is centered with h-full w-full on container. Tests verify centering classes. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `pnpm run check` passes with 0 errors
- `pnpm run test:run` passes all tests
- `pnpm run test:coverage` meets coverage thresholds
- Every sortable column has a floating filter visible in the column header
- YouTube icon is centered in its cell
</verification>

<success_criteria>
- All sortable/filterable columns have floating filters with correct types
- YouTube icon is vertically and horizontally centered
- All tests pass with coverage thresholds met
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-activity-page-beta-quality/03.2-06-SUMMARY.md`
</output>
