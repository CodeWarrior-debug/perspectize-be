---
phase: 03.4-perspectize-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/core/domain/user.go
  - backend/internal/core/ports/repositories/user_repository.go
  - backend/internal/core/services/user_service.go
  - backend/internal/adapters/repositories/postgres/gorm_models.go
  - backend/internal/adapters/repositories/postgres/gorm_mappers.go
  - backend/internal/adapters/repositories/postgres/gorm_user_repository.go
  - backend/migrations/000012_add_user_preferences.up.sql
  - backend/migrations/000012_add_user_preferences.down.sql
  - backend/schema.graphql
  - backend/internal/adapters/graphql/resolver/user_resolver.go
  - backend/test/services/user_service_test.go
  - backend/test/resolvers/content_resolver_test.go
autonomous: true

must_haves:
  truths:
    - "GraphQL updateUserPreferences mutation accepts userId, shape, color and returns updated User with preferences"
    - "User type in GraphQL includes preferences field returning JSONB data"
    - "preferences JSONB column exists on users table via migration 000012"
    - "All existing backend tests pass after adding UpdatePreferences to UserRepository interface"
  artifacts:
    - path: "backend/migrations/000012_add_user_preferences.up.sql"
      provides: "ALTER TABLE users ADD COLUMN preferences JSONB"
      contains: "preferences"
    - path: "backend/internal/core/domain/user.go"
      provides: "GlassesShape, GlassesColor enums, GlassesPreference, UserPreferences structs"
      contains: "GlassesPreference"
    - path: "backend/schema.graphql"
      provides: "UpdateGlassesPreferenceInput, updateUserPreferences mutation, preferences on User"
      contains: "updateUserPreferences"
  key_links:
    - from: "backend/internal/adapters/graphql/resolver/user_resolver.go"
      to: "backend/internal/core/services/user_service.go"
      via: "UpdatePreferences service call"
      pattern: "UpdatePreferences"
    - from: "backend/internal/adapters/repositories/postgres/gorm_user_repository.go"
      to: "backend/internal/adapters/repositories/postgres/gorm_models.go"
      via: "UserModel.Preferences json.RawMessage"
      pattern: "Preferences"
---

<objective>
Add JSONB preferences storage for glasses on the user table, with a GraphQL mutation to persist and retrieve glasses preferences.

Purpose: Backend foundation for the glasses identity feature — without this, glasses choices cannot persist.
Output: Working GraphQL mutation, migration, domain types, repository method, resolver.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-jeeves-branding-identity/03.4-RESEARCH.md
@.planning/phases/03.4-jeeves-branding-identity/REFERENCE-glasses.md

Key existing files to reference:
@backend/internal/core/domain/user.go
@backend/internal/core/ports/repositories/user_repository.go
@backend/internal/adapters/repositories/postgres/gorm_models.go
@backend/internal/adapters/repositories/postgres/gorm_mappers.go
@backend/internal/adapters/repositories/postgres/gorm_user_repository.go
@backend/schema.graphql
@backend/gqlgen.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain types, migration, GORM model, mapper, repository</name>
  <files>
    backend/internal/core/domain/user.go
    backend/internal/core/ports/repositories/user_repository.go
    backend/internal/core/services/user_service.go
    backend/internal/adapters/repositories/postgres/gorm_models.go
    backend/internal/adapters/repositories/postgres/gorm_mappers.go
    backend/internal/adapters/repositories/postgres/gorm_user_repository.go
    backend/migrations/000012_add_user_preferences.up.sql
    backend/migrations/000012_add_user_preferences.down.sql
    backend/test/services/user_service_test.go
    backend/test/resolvers/content_resolver_test.go
  </files>
  <action>
    1. **Domain types** — Add to `backend/internal/core/domain/user.go`:
       - `GlassesShape` string type with constants: `SUNGLASSES`, `ROUND`, `OVAL`
       - `GlassesColor` string type with constants: `purple`, `indigo`, `teal`, `rose`, `amber`, `slate`, `emerald`, `sky`
       - `GlassesPreference` struct with `Shape GlassesShape` and `Color GlassesColor` (json tags)
       - `UserPreferences` struct with `Glasses *GlassesPreference` (json tags, omitempty)
       - Add `Preferences *UserPreferences` field to `User` struct

    2. **Migration** — Create `000012_add_user_preferences.up.sql`:
       ```sql
       ALTER TABLE users ADD COLUMN preferences JSONB;
       ```
       And `000012_add_user_preferences.down.sql`:
       ```sql
       ALTER TABLE users DROP COLUMN preferences;
       ```

    3. **GORM model** — Add `Preferences json.RawMessage \`gorm:"type:jsonb"\`` to `UserModel` in `gorm_models.go`. Import `encoding/json` if not already imported.

    4. **Mapper** — In `gorm_mappers.go`:
       - Add `parseUserPreferences(raw json.RawMessage) *domain.UserPreferences` function (returns nil for empty/null, logs warning on parse error via slog.Warn — same pattern as existing content mappers)
       - Add `marshalUserPreferences(prefs *domain.UserPreferences) json.RawMessage` function (returns nil for nil prefs)
       - Update `userModelToDomain` to set `Preferences: parseUserPreferences(m.Preferences)`
       - Update `userDomainToModel` to set `Preferences: marshalUserPreferences(u.Preferences)`

    5. **Repository interface** — Add to `UserRepository` interface in `user_repository.go`:
       ```go
       UpdatePreferences(ctx context.Context, userID int, prefs *domain.UserPreferences) (*domain.User, error)
       ```
       Returns the updated user (consistent with existing Update method returning the fresh record).

    6. **Repository implementation** — Add `UpdatePreferences` to `GormUserRepository` in `gorm_user_repository.go`:
       - Marshal prefs to `json.RawMessage`
       - Use `r.db.WithContext(ctx).Model(&UserModel{}).Where("id = ?", userID).Update("preferences", data)`
       - Check `result.RowsAffected == 0` for `ErrNotFound`
       - On success, call `r.GetByID(ctx, userID)` to return the fresh user (same pattern as existing Create/Update methods)

    7. **Service** — Add `UpdatePreferences(ctx context.Context, userID int, prefs *domain.UserPreferences) (*domain.User, error)` to `UserService`. Delegate directly to repository (no extra business logic needed here).

    8. **Mock updates** — Update ALL `mockUserRepository` structs in test files. Search for them in:
       - `backend/test/services/user_service_test.go`
       - `backend/test/resolvers/content_resolver_test.go`
       - Any other files found via `grep -r "mockUserRepository" backend/test/`
       Add `UpdatePreferences` stub method that returns `nil, nil` by default, with an optional `updatePreferencesFn` field for test-specific behavior.

    9. **Verify compilation:** Run `cd backend && go build ./...` — must pass with zero errors.
  </action>
  <verify>
    `cd backend && go build ./...` passes with zero errors.
    `cd backend && go test ./...` passes (all existing tests still work).
  </verify>
  <done>
    Domain types exist. Migration 000012 created. GORM model has Preferences field. Mapper converts bidirectionally. Repository has UpdatePreferences. All mocks updated. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: GraphQL schema + resolver for updateUserPreferences</name>
  <files>
    backend/schema.graphql
    backend/internal/adapters/graphql/resolver/user_resolver.go
  </files>
  <action>
    1. **GraphQL schema** — In `backend/schema.graphql`:
       - Add `preferences: JSON` field to the `User` type (nullable — returns null for users without preferences)
       - Add input type:
         ```graphql
         input UpdateGlassesPreferenceInput {
           userId: IntID!
           shape: String!
           color: String!
         }
         ```
       - Add mutation: `updateUserPreferences(input: UpdateGlassesPreferenceInput!): User!`

    2. **Run codegen:** `cd backend && make graphql-gen` — this regenerates `models_gen.go` with the new input type.

    3. **Resolver** — In `user_resolver.go` (or wherever mutation resolvers live — check the generated resolver stubs after codegen), implement `UpdateUserPreferences`:
       - Extract `input.UserID`, `input.Shape`, `input.Color` from the generated input struct
       - Validate shape is one of "SUNGLASSES", "ROUND", "OVAL" — return error if not
       - Validate color is one of the 8 palette colors — return error if not
       - Build `domain.UserPreferences{Glasses: &domain.GlassesPreference{Shape: shape, Color: color}}`
       - Call `r.UserService.UpdatePreferences(ctx, userID, &prefs)`
       - Return the user, mapping domain.User to model.User (follow existing resolver patterns for user mapping)

    4. **User resolver preferences field** — Ensure the `User` type resolver maps `Preferences` from the domain user to the GraphQL `JSON` type. Since `JSON` maps to `graphql.Map`, convert `domain.UserPreferences` to `map[string]interface{}` by marshaling to JSON then unmarshaling to map. If the domain `Preferences` is nil, return nil.

    5. **Verify:** Run `cd backend && go build ./...` and `cd backend && go test ./...`.
  </action>
  <verify>
    `cd backend && go build ./...` passes.
    `cd backend && go test ./...` passes.
    Schema has `updateUserPreferences` mutation and `preferences` on User type.
  </verify>
  <done>
    GraphQL mutation `updateUserPreferences` works. Preferences field on User type returns JSONB data. Codegen ran. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` — zero errors
- `cd backend && go test ./...` — all tests pass
- Migration file 000012 exists with correct ALTER TABLE
- `grep "updateUserPreferences" backend/schema.graphql` returns the mutation
- `grep "preferences" backend/schema.graphql` shows field on User type
- `grep "UpdatePreferences" backend/internal/core/ports/repositories/user_repository.go` shows interface method
</verification>

<success_criteria>
- Backend compiles and all tests pass
- Migration 000012 adds preferences JSONB column to users table
- updateUserPreferences GraphQL mutation defined and implemented
- User GraphQL type includes preferences field
- Domain types for GlassesShape, GlassesColor, GlassesPreference, UserPreferences exist
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-jeeves-branding-identity/03.4-01-SUMMARY.md`
</output>
