---
phase: 03.4-perspectize-branding
plan: 03
type: execute
wave: 2
depends_on: ["03.4-01", "03.4-02"]
files_modified:
  - frontend/src/lib/components/GlassesPickerPopover.svelte
  - frontend/src/lib/queries/users.ts
  - frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts
  - frontend/tests/components/GlassesPickerPopover.test.ts
  - frontend/tests/unit/queries-users.test.ts
autonomous: true

must_haves:
  truths:
    - "GlassesPickerPopover shows shape toggle with 3 mini icon previews and color swatches with 8 palette colors"
    - "Inline preview updates live as user toggles shape and color"
    - "Save button fires updateUserPreferences mutation and closes popover"
    - "Skip/dismiss button closes popover without saving"
    - "LIST_USERS query includes preferences field"
  artifacts:
    - path: "frontend/src/lib/components/GlassesPickerPopover.svelte"
      provides: "Picker popover with shape toggle, color swatches, live preview, save/skip"
      min_lines: 60
    - path: "frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts"
      provides: "TanStack mutation hook for updateUserPreferences"
      exports: ["useUpdateGlassesPreference"]
    - path: "frontend/src/lib/queries/users.ts"
      provides: "UPDATE_GLASSES_PREFERENCE GQL string, updated LIST_USERS with preferences"
      contains: "UPDATE_GLASSES_PREFERENCE"
  key_links:
    - from: "frontend/src/lib/components/GlassesPickerPopover.svelte"
      to: "frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts"
      via: "useUpdateGlassesPreference() call on save"
      pattern: "useUpdateGlassesPreference"
    - from: "frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts"
      to: "frontend/src/lib/queries/users.ts"
      via: "import UPDATE_GLASSES_PREFERENCE"
      pattern: "UPDATE_GLASSES_PREFERENCE"
    - from: "frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts"
      to: "frontend/src/lib/queries/keys.ts"
      via: "queryKeys.users.list() invalidation on success"
      pattern: "queryKeys.users.list"
---

<objective>
Build the GlassesPickerPopover UI component and the mutation hook to persist glasses preferences via GraphQL.

Purpose: Users need a way to pick their glasses shape and color, and those choices need to persist to the backend.
Output: GlassesPickerPopover.svelte, useUpdateGlassesPreference hook, updated queries/users.ts with mutation + preferences field.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.4-jeeves-branding-identity/03.4-RESEARCH.md
@.planning/phases/03.4-jeeves-branding-identity/03.4-01-SUMMARY.md
@.planning/phases/03.4-jeeves-branding-identity/03.4-02-SUMMARY.md

Key existing files:
@frontend/src/lib/components/FormPopover.svelte (popover pattern — but do NOT use FormPopover, use raw Popover primitives)
@frontend/src/lib/queries/hooks/useCreateUser.ts (mutation hook pattern to follow)
@frontend/src/lib/queries/users.ts (add mutation GQL here)
@frontend/src/lib/queries/keys.ts (query key factory)
@frontend/src/lib/components/GlassesIcon.svelte (from Plan 02)
@frontend/src/lib/types/glasses.ts (from Plan 02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: GraphQL mutation definition and mutation hook</name>
  <files>
    frontend/src/lib/queries/users.ts
    frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts
  </files>
  <action>
    1. **Update `frontend/src/lib/queries/users.ts`:**
       - Add `UpdateGlassesPreferenceInput` TypeScript interface: `{ userId: number; shape: string; color: string }`
       - Add `UPDATE_GLASSES_PREFERENCE` GQL mutation string:
         ```graphql
         mutation UpdateGlassesPreference($input: UpdateGlassesPreferenceInput!) {
           updateUserPreferences(input: $input) {
             id
             username
             preferences
           }
         }
         ```
       - Update `LIST_USERS` query to include `preferences` field (add it after `username`). The `preferences` field returns a JSON object — small payload, needed for glasses display.

    2. **Create `frontend/src/lib/queries/hooks/useUpdateGlassesPreference.ts`:**
       - Follow the exact pattern of `useCreateUser.ts`
       - Import `createMutation`, `useQueryClient` from `@tanstack/svelte-query`
       - Import `toast` from `svelte-sonner`
       - Import `graphqlClient` from `../client`
       - Import `UPDATE_GLASSES_PREFERENCE`, `UpdateGlassesPreferenceInput` from `../users`
       - Import `queryKeys` from `../keys`
       - `mutationFn`: call `graphqlClient.request(UPDATE_GLASSES_PREFERENCE, { input })`
       - `onSuccess`: invalidate `queryKeys.users.list()` (refreshes UserSelector with new preferences)
       - `onError`: `toast.error('Failed to save glasses preference.')`
       - Export the function as `useUpdateGlassesPreference`
  </action>
  <verify>
    `cd frontend && pnpm run check` — TypeScript passes.
    `grep "preferences" frontend/src/lib/queries/users.ts` shows the field in LIST_USERS.
  </verify>
  <done>
    UPDATE_GLASSES_PREFERENCE mutation defined. LIST_USERS includes preferences. Mutation hook follows existing project patterns with cache invalidation and error toast.
  </done>
</task>

<task type="auto">
  <name>Task 2: GlassesPickerPopover component and tests</name>
  <files>
    frontend/src/lib/components/GlassesPickerPopover.svelte
    frontend/tests/components/GlassesPickerPopover.test.ts
    frontend/tests/unit/queries-users.test.ts
  </files>
  <action>
    1. **Create `GlassesPickerPopover.svelte`** using RAW Popover primitives (NOT FormPopover — this is a picker, not a form):
       - Import `Popover`, `PopoverTrigger`, `PopoverContent` from `$lib/components/shadcn`
       - Import `GlassesIcon` from `$lib/components/GlassesIcon.svelte`
       - Import `useUpdateGlassesPreference` from `$lib/queries/hooks/useUpdateGlassesPreference`
       - Import types from `$lib/types/glasses`

       **Props** (Svelte 5 `$props()`):
       - `userId: number` — required
       - `initialShape?: GlassesShape` — default `'sunglasses'`
       - `initialColor?: GlassesColor` — default `'purple'`
       - `open?: boolean` — `$bindable(false)` for external control
       - `onDismiss?: () => void` — callback for skip/dismiss (onboarding mode)
       - `triggerClass?: string` — optional class for trigger button

       **State:**
       - `let selectedShape = $state<GlassesShape>(initialShape)`
       - `let selectedColor = $state<GlassesColor>(initialColor)`
       - Reset selected values when `open` changes to true (use `$effect`)

       **Layout (inside PopoverContent, class="w-64 p-4"):**
       - **Live preview** — centered `GlassesIcon` at `size={48}` showing `selectedShape` + `selectedColor`
       - **Shape toggle** — 3 mini cards in a flex row (`flex gap-2 mb-3`). Each card is a `<button>` containing a `GlassesIcon` at `size={24}` with the respective shape. Active card has `border-primary bg-accent`, inactive has `border-border`. Use `aria-pressed` for accessibility.
       - **Color swatches** — 8 circular buttons in a flex-wrap row (`flex gap-2 flex-wrap mb-4`). Each is `w-7 h-7 rounded-full`. Background color is the light-mode `from` value from COLOR_PALETTE. Selected swatch has `border-foreground` ring, others have `border-transparent`. Use `aria-label` with color name and `aria-pressed`.
       - **Actions** — flex row with justify-end:
         - If `onDismiss` provided: show "Skip" text button (`text-sm text-muted-foreground`)
         - "Save" button (styled with primary button classes). Disabled while `mutation.isPending`.

       **Save handler:** Call `mutation.mutate({ userId, shape: selectedShape.toUpperCase(), color: selectedColor })` then set `open = false`. Note: backend expects shape as uppercase (SUNGLASSES, ROUND, OVAL) but frontend stores lowercase.

       **Dismiss handler:** Call `onDismiss?.()` then set `open = false`.

    2. **Create tests** at `frontend/tests/components/GlassesPickerPopover.test.ts`:
       - Test that component renders without errors
       - Test that 3 shape toggle buttons exist
       - Test that 8 color swatch buttons exist
       - Test that clicking a shape button updates the preview (check aria-pressed changes)

    3. **Update `frontend/tests/unit/queries-users.test.ts`:**
       - Add test that `UPDATE_GLASSES_PREFERENCE` mutation string contains expected operation name
       - Add test that `LIST_USERS` query string contains `preferences` field

    4. **Run tests:** `cd frontend && pnpm run test:run` and `cd frontend && pnpm run test:coverage`.
  </action>
  <verify>
    `cd frontend && pnpm run test:run` — all tests pass.
    `cd frontend && pnpm run test:coverage` — coverage thresholds met.
    GlassesPickerPopover.svelte exists with shape toggle + color swatches + save/skip.
  </verify>
  <done>
    GlassesPickerPopover renders picker UI with live preview, shape toggle, color swatches, save/skip actions. Mutation hook wired. All tests pass with coverage thresholds met.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm run check` — TypeScript passes
- `cd frontend && pnpm run test:run` — all tests pass
- `cd frontend && pnpm run test:coverage` — coverage thresholds met
- GlassesPickerPopover.svelte uses raw Popover primitives (NOT FormPopover)
- UPDATE_GLASSES_PREFERENCE mutation defined in users.ts
- LIST_USERS query includes preferences field
- useUpdateGlassesPreference hook invalidates queryKeys.users.list() on success
</verification>

<success_criteria>
- Picker popover shows 3 shape options and 8 color swatches
- Live preview updates as user toggles shape and color
- Save fires mutation, Skip calls onDismiss callback
- LIST_USERS returns preferences for all users
- All frontend tests pass with coverage thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-jeeves-branding-identity/03.4-03-SUMMARY.md`
</output>
