---
phase: 03.5-google-taxonomy-research-spike
plan: 02
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified:
  - .planning/phases/03.5-google-taxonomy-research-spike/CURATED-CATEGORIES.md
  - .planning/phases/03.5-google-taxonomy-research-spike/seed-data/categories-seed.sql
  - .planning/phases/03.5-google-taxonomy-research-spike/seed-data/category-youtube-mapping.md
autonomous: false

must_haves:
  truths:
    - "Curated category list documents every category selected for Perspectize with ltree paths and display names"
    - "SQL seed file is ready to execute against PostgreSQL with ltree extension and inserts all curated categories"
    - "YouTube category mapping document shows how YouTube's 32 categories map to the curated Google NL categories"
    - "User has confirmed the curated list matches their exploration findings"
  artifacts:
    - path: ".planning/phases/03.5-google-taxonomy-research-spike/CURATED-CATEGORIES.md"
      provides: "Final curated category list with hierarchy visualization"
      contains: "ltree"
    - path: ".planning/phases/03.5-google-taxonomy-research-spike/seed-data/categories-seed.sql"
      provides: "PostgreSQL seed data for categories table"
      contains: "INSERT INTO categories"
    - path: ".planning/phases/03.5-google-taxonomy-research-spike/seed-data/category-youtube-mapping.md"
      provides: "YouTube category ID to Google NL category mapping"
      contains: "YouTube"
  key_links:
    - from: "CURATED-CATEGORIES.md"
      to: "seed-data/categories-seed.sql"
      via: "Every category in the document has a corresponding INSERT"
      pattern: "INSERT INTO"
    - from: "seed-data/category-youtube-mapping.md"
      to: "CURATED-CATEGORIES.md"
      via: "Mapping references categories from the curated list"
      pattern: "arts_and_entertainment|sports|games"
---

<objective>
Produce the final curated category taxonomy, ltree seed SQL, and YouTube mapping document -- the deliverables that unblock Phase 13 (Content Categories) implementation.

Purpose: Transform research findings into implementation-ready artifacts. The curated list becomes Phase 13's seed migration. The YouTube mapping becomes the auto-classification reference.
Output: Curated category document, SQL seed file, YouTube mapping document
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.5-google-taxonomy-research-spike/03.5-RESEARCH.md
@.planning/phases/13-content-categories/13-CONTEXT.md
@.planning/phases/03.5-google-taxonomy-research-spike/03.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Initial Curated Categories + Seed SQL + YouTube Mapping</name>
  <files>
    .planning/phases/03.5-google-taxonomy-research-spike/CURATED-CATEGORIES.md
    .planning/phases/03.5-google-taxonomy-research-spike/seed-data/categories-seed.sql
    .planning/phases/03.5-google-taxonomy-research-spike/seed-data/category-youtube-mapping.md
  </files>
  <action>
Generate three artifacts from the RESEARCH.md data. These are initial versions the user will review.

**CURATED-CATEGORIES.md:**
- Title: "Perspectize Content Categories -- Curated Taxonomy"
- Show the 20 curated top-level categories (from RESEARCH.md "Recommended Curated Categories")
- For each top-level category, include 2-3 levels of subcategories from the "Known Subcategory Structure" section
- Format as a tree visualization using indentation
- For each category show: display name, ltree path, source (google_nl)
- Include a "Depth Decision" section recommending 3 levels (top + 2 sub) for initial seed
- Include a "Custom Category Slots" section noting where user categories would extend the tree (e.g., team-specific categories under sports)
- Total curated count should be approximately 60-80 categories (20 top-level + their known subcategories to 2-3 levels deep)

**categories-seed.sql:**
- Assumes the schema from RESEARCH.md Pattern 2 (CREATE TABLE categories with ltree)
- Include `CREATE EXTENSION IF NOT EXISTS ltree;` at top
- Include the CREATE TABLE statement
- Include all indexes
- INSERT statements for every category in CURATED-CATEGORIES.md
- Each INSERT: (name, slug, path, source, google_path)
- Use the GoogleNLPathToLtree transformation rules from RESEARCH.md for path values
- Slug: lowercase, hyphens instead of underscores, no special chars
- Wrap in a transaction (BEGIN; ... COMMIT;)
- Add comment headers for each top-level category section

**category-youtube-mapping.md:**
- Map YouTube Data API's 32 category IDs to the curated Google NL categories
- Use the mapping table from RESEARCH.md "YouTube Category Mapping" section
- For each YouTube category: ID, YouTube name, primary Google NL ltree path, secondary paths (if applicable)
- Include classification strategy summary (from RESEARCH.md): concatenate metadata, V2 model, confidence thresholds
- Include fallback logic: if Google NL confidence < 0.5, use YouTube category ID mapping

Use the transformation rules from RESEARCH.md exactly:
1. Remove leading `/`
2. Replace `/` with `.`
3. Replace ` & ` with `_and_`
4. Replace ` ` with `_`
5. Remove commas
6. Lowercase
7. Remove remaining non-alphanumeric except `_` and `-`
  </action>
  <verify>
Verify SQL syntax: `python3 -c "
content = open('.planning/phases/03.5-google-taxonomy-research-spike/seed-data/categories-seed.sql').read()
assert 'CREATE EXTENSION' in content, 'Missing ltree extension'
assert 'CREATE TABLE categories' in content, 'Missing table creation'
assert 'INSERT INTO categories' in content, 'Missing inserts'
assert 'BEGIN' in content and 'COMMIT' in content, 'Missing transaction'
inserts = content.count('INSERT INTO categories')
assert inserts >= 40, f'Only {inserts} inserts, expected 40+'
print(f'Valid: {inserts} category inserts found')
"`
  </verify>
  <done>Three artifacts exist: curated categories document with tree visualization, SQL seed file with 40+ INSERT statements in a transaction, YouTube mapping document with all 13 mapped YouTube categories</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Initial curated category taxonomy, ltree seed SQL, and YouTube category mapping based on the RESEARCH.md findings. These artifacts are ready for the user to review against their Postman exploration results.</what-built>
  <how-to-verify>
1. Open `.planning/phases/03.5-google-taxonomy-research-spike/CURATED-CATEGORIES.md`
   - Review the category tree -- does it cover the content types you rate on YouTube?
   - Are there missing subcategories you discovered in Postman exploration?
   - Is the depth (3 levels) right, or do you want deeper/shallower?

2. Open `.planning/phases/03.5-google-taxonomy-research-spike/seed-data/categories-seed.sql`
   - Skim the INSERT statements -- do the ltree paths look correct?
   - Are display names readable?

3. Open `.planning/phases/03.5-google-taxonomy-research-spike/seed-data/category-youtube-mapping.md`
   - Does the YouTube-to-Google-NL mapping make sense for your videos?
   - Any YouTube categories you use that aren't mapped?

4. Compare against your Postman exploration findings:
   - Any categories you discovered that are missing from the curated list?
   - Any categories in the list that seem irrelevant based on actual API responses?
   - Any confidence threshold adjustments needed?
  </how-to-verify>
  <resume-signal>Type "approved" to finalize, or describe changes needed (add categories, remove categories, adjust depth, etc.)</resume-signal>
</task>

</tasks>

<verification>
1. CURATED-CATEGORIES.md has 20 top-level categories with subcategories
2. categories-seed.sql has valid SQL with BEGIN/COMMIT, ltree extension, and 40+ inserts
3. category-youtube-mapping.md maps YouTube category IDs to Google NL ltree paths
4. User has approved the curated list against their exploration findings
</verification>

<success_criteria>
- All three artifacts exist and are internally consistent (same categories across all three docs)
- ltree paths follow the transformation rules exactly
- SQL is ready to copy into a Phase 13 migration file
- YouTube mapping provides a clear fallback classification strategy
- User has confirmed the taxonomy matches their content rating needs
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-google-taxonomy-research-spike/03.5-02-SUMMARY.md`
</output>
