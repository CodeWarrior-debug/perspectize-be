---
phase: 08.1-api-schema-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/schema.graphql
  - backend/internal/adapters/graphql/generated/generated.go
  - backend/internal/adapters/graphql/model/models_gen.go
  - backend/internal/adapters/graphql/resolvers/schema.resolvers.go
autonomous: true

must_haves:
  truths:
    - "Timestamps in GraphQL responses are RFC3339Nano strings (gqlgen Time scalar format)"
    - "contentType field accepts only 'YOUTUBE' enum value, not arbitrary strings"
    - "deletePerspective mutation accepts IntID (database integer) instead of ID scalar"
  artifacts:
    - path: "backend/schema.graphql"
      provides: "Time scalar, ContentType enum, deletePerspective IntID parameter"
      contains: "scalar Time"
    - path: "backend/internal/adapters/graphql/generated/generated.go"
      provides: "Generated GraphQL executable with Time scalar support"
      min_lines: 1000
  key_links:
    - from: "backend/schema.graphql"
      to: "backend/internal/adapters/graphql/generated/"
      via: "gqlgen code generation"
      pattern: "go run github.com/99designs/gqlgen"
---

<objective>
Fix GraphQL schema type quality: migrate timestamps to DateTime scalar, enforce ContentType enum, fix deletePerspective parameter type

Purpose: Eliminate type correctness issues (H-04, H-05, M-04) by using proper GraphQL scalars and enums instead of loose String types
Output: Updated schema.graphql with Time scalar, ContentType enum enforcement, and IntID for deletePerspective; regenerated GraphQL code
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jamesjordan/GitHub/perspectize/.planning/PROJECT.md
@/Users/jamesjordan/GitHub/perspectize/.planning/ROADMAP.md
@/Users/jamesjordan/GitHub/perspectize/.planning/STATE.md
@/Users/jamesjordan/GitHub/perspectize/.planning/phases/08.1-api-schema-quality/08.1-RESEARCH.md

# Codebase context
@/Users/jamesjordan/GitHub/perspectize/.planning/codebase/ARCHITECTURE.md
@/Users/jamesjordan/GitHub/perspectize/.planning/codebase/CONVENTIONS.md
@/Users/jamesjordan/GitHub/perspectize/.planning/codebase/STACK.md

# Current schema
@/Users/jamesjordan/GitHub/perspectize/backend/schema.graphql
@/Users/jamesjordan/GitHub/perspectize/backend/gqlgen.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate timestamp fields to Time scalar</name>
  <files>backend/schema.graphql</files>
  <action>
Replace all timestamp String! fields with Time! scalar in schema.graphql.

Current state (lines 18-19, 63-64, 77, 90-91):
- User.createdAt: String!
- User.updatedAt: String!
- Content.createdAt: String!
- Content.updatedAt: String!
- Content.publishedAt: String
- Perspective.createdAt: String!
- Perspective.updatedAt: String!

Changes:
1. Add scalar declaration at top of file: `scalar Time`
2. Change all timestamp fields from String! to Time!
3. Keep publishedAt as Time (nullable, not Time!)

gqlgen built-in Time scalar maps to Go time.Time and serializes to RFC3339Nano format automatically (RESEARCH.md Pattern 1). No gqlgen.yml configuration needed.

**Breaking change accepted:** Project is pre-production with no external clients. Document in commit message.
  </action>
  <verify>
Verify scalar declaration and field changes:
```bash
grep "scalar Time" backend/schema.graphql
grep -E "(createdAt|updatedAt|publishedAt): Time" backend/schema.graphql
```
All timestamp fields should show Time or Time! (not String).
  </verify>
  <done>schema.graphql declares scalar Time and all timestamp fields use Time/Time! scalar</done>
</task>

<task type="auto">
  <name>Task 2: Enforce ContentType enum in schema</name>
  <files>backend/schema.graphql</files>
  <action>
Change Content.contentType from String! to ContentType! enum.

Current state (schema.graphql line 77):
```graphql
type Content {
  contentType: String!
}
```

Change to:
```graphql
type Content {
  contentType: ContentType!
}
```

The ContentType enum already exists in schema (line 171-173):
```graphql
enum ContentType {
  YOUTUBE
}
```

Domain binding already exists in gqlgen.yml (confirmed in RESEARCH.md Pattern "GraphQL Enum Handling"):
```yaml
models:
  ContentType:
    model:
      - github.com/CodeWarrior-debug/perspectize/backend/internal/core/domain.ContentType
```

This change enforces type safety at GraphQL layer — only "YOUTUBE" accepted, not arbitrary strings.
  </action>
  <verify>
```bash
grep "contentType: ContentType!" backend/schema.graphql
```
Field should show ContentType! enum, not String!.
  </verify>
  <done>Content.contentType field uses ContentType! enum</done>
</task>

<task type="auto">
  <name>Task 3: Fix deletePerspective parameter to IntID</name>
  <files>backend/schema.graphql</files>
  <action>
Change deletePerspective mutation parameter from ID! to IntID! scalar.

Current state (schema.graphql line ~130):
```graphql
deletePerspective(id: ID!): Boolean!
```

Change to:
```graphql
deletePerspective(id: IntID!): Boolean!
```

IntID scalar already defined in schema (line 166):
```graphql
scalar IntID
```

IntID maps to Go int type via pkg/graphql/intid.go (custom scalar for database integers). Using IntID for delete operation matches pattern used in other mutations (createPerspective, updatePerspective both use IntID for contentId).

**Consistency fix:** All perspective mutations now use IntID for integer database IDs, only top-level query IDs use String (converted via strconv.Atoi in resolver).
  </action>
  <verify>
```bash
grep "deletePerspective(id: IntID!)" backend/schema.graphql
```
Mutation signature should show IntID! parameter.
  </verify>
  <done>deletePerspective mutation uses IntID! parameter type</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate GraphQL code</name>
  <files>backend/internal/adapters/graphql/generated/generated.go, backend/internal/adapters/graphql/model/models_gen.go, backend/internal/adapters/graphql/resolvers/schema.resolvers.go</files>
  <action>
Run gqlgen code generation to reflect schema changes.

```bash
cd backend
go run github.com/99designs/gqlgen generate
```

Expected changes:
- generated/generated.go: Updated with Time scalar marshaling/unmarshaling
- model/models_gen.go: Content.ContentType now uses domain.ContentType enum, timestamps use time.Time
- resolvers/schema.resolvers.go: deletePerspective parameter type updated to int (IntID)

**Note:** Resolver implementations may need adjustment after generation. If gqlgen generates stub methods, preserve existing resolver logic.

After generation, verify build:
```bash
cd backend
go build ./...
```

If compilation errors occur, resolver functions need signature updates (Task 5 in next plan will handle resolver implementation adjustments).
  </action>
  <verify>
```bash
cd backend
go build ./...
echo "Exit code: $?"
```
Build should succeed (exit code 0). Generated files should exist with updated types.
  </verify>
  <done>gqlgen generated code reflects schema changes, backend builds successfully</done>
</task>

</tasks>

<verification>
**Schema correctness:**
1. `scalar Time` declared in schema.graphql
2. All timestamp fields use Time or Time! (7 fields across User, Content, Perspective)
3. Content.contentType uses ContentType! enum
4. deletePerspective uses IntID! parameter
5. gqlgen generated code compiles without errors

**Breaking change documented:**
- Timestamp fields changed from String to Time (RFC3339Nano format preserved)
- This is a breaking change for clients expecting string timestamps
- Acceptable because project is pre-production with no external clients

**Test command:**
```bash
cd backend
go build ./... && echo "✓ Build successful"
```
</verification>

<success_criteria>
1. schema.graphql declares scalar Time at top level
2. All 7 timestamp fields (User.createdAt/updatedAt, Content.createdAt/updatedAt/publishedAt, Perspective.createdAt/updatedAt) use Time scalar
3. Content.contentType field uses ContentType! enum instead of String!
4. deletePerspective mutation uses IntID! parameter instead of ID!
5. gqlgen generate completes without errors
6. Backend builds successfully (go build ./...)
7. Generated files exist: generated/generated.go, model/models_gen.go, resolvers/schema.resolvers.go
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-api-schema-quality/08.1-01-SUMMARY.md`
</output>
