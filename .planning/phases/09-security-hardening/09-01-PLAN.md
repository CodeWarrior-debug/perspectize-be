---
phase: 09-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/go.mod
  - backend/go.sum
  - backend/internal/adapters/web/middleware/auth.go
  - backend/internal/core/domain/auth.go
  - backend/internal/core/ports/services/auth_service.go
  - backend/internal/core/services/auth_service.go
  - backend/internal/config/security.go
  - backend/cmd/server/main.go
  - backend/internal/adapters/graphql/directives/auth.go
  - backend/schema.graphql
autonomous: true

must_haves:
  truths:
    - "JWT tokens can be generated with user claims (user ID, email, expiration)"
    - "JWT tokens can be validated and user context extracted"
    - "Authentication middleware runs before GraphQL handler and stores user context"
    - "GraphQL directives can enforce authentication on fields"
    - "Invalid/missing tokens result in unauthenticated requests (allowed to pass through)"
    - "Secret key is loaded from environment variable"
  artifacts:
    - path: "backend/internal/adapters/web/middleware/auth.go"
      provides: "JWT validation middleware with context storage"
      min_lines: 60
      exports: ["AuthMiddleware", "ForContext"]
    - path: "backend/internal/core/services/auth_service.go"
      provides: "JWT generation and validation logic"
      exports: ["AuthService", "NewAuthService"]
    - path: "backend/internal/adapters/graphql/directives/auth.go"
      provides: "GraphQL @auth directive implementation"
      exports: ["Auth"]
    - path: "backend/internal/config/security.go"
      provides: "Security configuration structure"
      contains: "type Security struct"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "middleware/auth.go"
      via: "r.Use(auth.AuthMiddleware(jwtSecret))"
      pattern: "r\\.Use.*AuthMiddleware"
    - from: "backend/internal/adapters/graphql/directives/auth.go"
      to: "middleware/auth.go"
      via: "ForContext() to extract user"
      pattern: "middleware\\.ForContext"
---

<objective>
Establish JWT-based authentication infrastructure with httpOnly cookies, middleware-based validation, and directive-based authorization foundation.

Purpose: Create the authentication layer that all other security controls depend on. This plan implements the JWT generation/validation service, HTTP middleware for token validation, GraphQL directives for authorization, and wires everything together in the server startup.

Output: Working authentication infrastructure ready for protecting mutations and queries. No login/register endpoints yet (deferred to Phase 10) — this plan focuses on the auth machinery itself.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-hardening/09-RESEARCH.md
@backend/cmd/server/main.go
@backend/internal/config/config.go
@backend/schema.graphql
</context>

<tasks>

<task type="auto">
  <name>Install JWT dependencies and create auth domain models</name>
  <files>
    backend/go.mod
    backend/go.sum
    backend/internal/core/domain/auth.go
  </files>
  <action>
    1. Install golang-jwt/jwt v5 and argon2:
       ```bash
       cd backend
       go get github.com/golang-jwt/jwt/v5
       go get golang.org/x/crypto/argon2
       ```

    2. Create domain/auth.go with JWT claims structure:
       - Claims struct with UserID (int), Email (string), jwt.RegisteredClaims
       - No business logic here, pure domain model

    Per research: golang-jwt/jwt v5 is the maintained JWT library (successor to dgrijalva/jwt-go)
  </action>
  <verify>
    go list -m github.com/golang-jwt/jwt/v5
    go list -m golang.org/x/crypto/argon2
    grep -q "type Claims struct" backend/internal/core/domain/auth.go
  </verify>
  <done>JWT dependencies installed, Claims domain model exists</done>
</task>

<task type="auto">
  <name>Create AuthService for JWT generation and validation</name>
  <files>
    backend/internal/core/ports/services/auth_service.go
    backend/internal/core/services/auth_service.go
  </files>
  <action>
    1. Create ports/services/auth_service.go interface:
       - GenerateAccessToken(userID int, email string) (string, error)
       - ValidateToken(tokenString string) (*domain.Claims, error)

    2. Create services/auth_service.go implementation:
       - Constructor: NewAuthService(jwtSecret []byte, accessTokenTTL time.Duration) *AuthService
       - GenerateAccessToken: Use jwt.NewWithClaims with HS256, set Issuer="perspectize", ExpiresAt=now+TTL
       - ValidateToken: Parse with jwt.ParseWithClaims, validate signing method is HMAC, return claims
       - If secret is <32 bytes, log warning at startup (security best practice)

    Per research Pattern 1: JWT signing with proper algorithm validation and error handling
  </action>
  <verify>
    go build ./internal/core/services/
    grep -q "GenerateAccessToken" backend/internal/core/ports/services/auth_service.go
    grep -q "ValidateToken" backend/internal/core/ports/services/auth_service.go
  </verify>
  <done>AuthService interface and implementation exist, can generate/validate JWTs</done>
</task>

<task type="auto">
  <name>Create authentication middleware for HTTP layer</name>
  <files>
    backend/internal/adapters/web/middleware/auth.go
  </files>
  <action>
    Create middleware/auth.go with:

    1. Context key: `const userContextKey contextKey = "user"`

    2. AuthMiddleware(authService AuthService) func(http.Handler) http.Handler:
       - Extract token from httpOnly cookie "auth_token"
       - If no cookie: call next() (unauthenticated request allowed)
       - If cookie exists: call authService.ValidateToken()
       - If valid: store user ID in context via context.WithValue(r.Context(), userContextKey, claims.UserID)
       - If invalid: call next() (treat as unauthenticated, no error response)

    3. ForContext(ctx context.Context) (int, bool):
       - Helper to extract user ID from context in resolvers/directives
       - Returns (userID, authenticated bool)

    Per research Pattern 1: Middleware authenticates, directives authorize. Never block unauthenticated requests in middleware.
  </action>
  <verify>
    go build ./internal/adapters/web/middleware/
    grep -q "AuthMiddleware" backend/internal/adapters/web/middleware/auth.go
    grep -q "ForContext" backend/internal/adapters/web/middleware/auth.go
  </verify>
  <done>Auth middleware extracts JWT from cookie, stores user context</done>
</task>

<task type="auto">
  <name>Create GraphQL directives for authorization</name>
  <files>
    backend/schema.graphql
    backend/internal/adapters/graphql/directives/auth.go
  </files>
  <action>
    1. Add directives to schema.graphql:
       ```graphql
       directive @auth on FIELD_DEFINITION
       directive @owner(idField: String!) on FIELD_DEFINITION
       ```

    2. Create directives/auth.go with DirectiveRoot:
       - Auth(ctx, obj, next) method:
         - Call middleware.ForContext(ctx) to get userID
         - If not authenticated: return nil, fmt.Errorf("access denied: authentication required")
         - If authenticated: return next(ctx)

       - Owner(ctx, obj, next, idField) method:
         - Call middleware.ForContext(ctx)
         - If not authenticated: return error
         - Extract resource ID from fieldContext.Args[idField]
         - Check ownership (for now, just validate authenticated - full ownership in Plan 02)
         - Return next(ctx)

    3. Run `make graphql-gen` to regenerate schema with directives

    Per research Pattern 2: Directive-based authorization visible in schema, runs after middleware authentication
  </action>
  <verify>
    grep -q "directive @auth" backend/schema.graphql
    grep -q "directive @owner" backend/schema.graphql
    make -C backend graphql-gen
    go build ./internal/adapters/graphql/directives/
  </verify>
  <done>GraphQL directives defined, implemented, and generated</done>
</task>

<task type="auto">
  <name>Wire authentication in server startup</name>
  <files>
    backend/internal/config/security.go
    backend/cmd/server/main.go
  </files>
  <action>
    1. Create config/security.go:
       - Security struct with JWTSecret string, AccessTokenMinutes int
       - Load from env: JWT_SECRET (required in prod), ACCESS_TOKEN_MINUTES (default 15)
       - Validate JWT_SECRET length >=32 bytes at startup, fail-fast in production

    2. Update cmd/server/main.go:
       - Load security config after database
       - Create authService: services.NewAuthService([]byte(cfg.Security.JWTSecret), time.Duration(cfg.Security.AccessTokenMinutes)*time.Minute)
       - Add auth middleware AFTER RealIP, BEFORE RequestTimer: `r.Use(authmw.AuthMiddleware(authService))`
       - Wire directives to resolver config:
         ```go
         config := generated.Config{
             Resolvers: resolver,
             Directives: generated.DirectiveRoot{
                 Auth: directives.NewDirectiveRoot().Auth,
                 Owner: directives.NewDirectiveRoot().Owner,
             },
         }
         ```

    Per research Pitfall 6: Rate limiting runs first, but auth middleware MUST run before GraphQL handler
  </action>
  <verify>
    go build ./cmd/server/
    grep -q "AuthMiddleware" backend/cmd/server/main.go
    grep -q "Security" backend/internal/config/security.go
  </verify>
  <done>Auth service created, middleware wired, directives connected, server compiles</done>
</task>

<task type="auto">
  <name>Add @auth directive to protected mutations in schema</name>
  <files>
    backend/schema.graphql
  </files>
  <action>
    Add @auth directive to all mutations (users can only perform actions when authenticated):

    ```graphql
    type Mutation {
      createUser(input: CreateUserInput!): User! @auth
      createContentFromYouTube(input: CreateContentFromYouTubeInput!): Content! @auth
      deleteContent(id: IntID!): Boolean! @auth
      createPerspective(input: CreatePerspectiveInput!): Perspective! @auth
      updatePerspective(id: IntID!, input: UpdatePerspectiveInput!): Perspective! @auth
      deletePerspective(id: IntID!): Boolean! @auth
    }
    ```

    Run `make graphql-gen` to regenerate schema.

    Note: Queries remain unauthenticated for now (public read). Authorization for email visibility comes in Plan 02.
  </action>
  <verify>
    grep -q "createUser.*@auth" backend/schema.graphql
    grep -q "createContentFromYouTube.*@auth" backend/schema.graphql
    make -C backend graphql-gen
  </verify>
  <done>All mutations protected by @auth directive</done>
</task>

<task type="auto">
  <name>Verify build and add .env.example entry</name>
  <files>
    backend/.env.example
  </files>
  <action>
    1. Add to .env.example:
       ```
       # JWT Authentication (required in production)
       JWT_SECRET=your-secret-key-here-minimum-32-bytes-for-security
       ACCESS_TOKEN_MINUTES=15
       ```

    2. Build and verify:
       ```bash
       cd backend
       make build
       go test ./internal/core/services/... -run TestAuth
       ```

    Note: No login/register mutations yet — those come in Phase 10. This plan establishes the auth machinery.
  </action>
  <verify>
    grep -q "JWT_SECRET" backend/.env.example
    make -C backend build
  </verify>
  <done>Auth infrastructure compiles, .env documented</done>
</task>

</tasks>

<verification>
1. Build passes: `cd backend && make build`
2. JWT generation works (unit test)
3. Middleware extracts user context
4. Directives enforce @auth on mutations
5. Server starts without errors
6. Unauthenticated GraphQL request to mutation returns "access denied"
</verification>

<success_criteria>
- [ ] golang-jwt/jwt v5 installed
- [ ] AuthService interface and implementation exist
- [ ] JWT generation includes user claims and expiration
- [ ] JWT validation returns claims or error
- [ ] Auth middleware stores user ID in context when token valid
- [ ] ForContext helper extracts user from context
- [ ] @auth and @owner directives defined in schema
- [ ] Directive implementations check authentication
- [ ] All mutations have @auth directive
- [ ] Security config loads JWT_SECRET from env
- [ ] JWT_SECRET validated as >=32 bytes in production
- [ ] Auth middleware wired in server startup BEFORE GraphQL handler
- [ ] Directives wired to resolver config
- [ ] Backend builds with no errors
- [ ] .env.example documents JWT_SECRET and ACCESS_TOKEN_MINUTES
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-hardening/09-01-SUMMARY.md`
</output>
