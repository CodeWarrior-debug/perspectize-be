---
phase: 12-authentication
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03", "12-04"]
files_modified:
  - backend/.env.example
  - frontend/.env.example
  - frontend/src/lib/components/AddVideoPopover.svelte
  - frontend/src/lib/components/ActivityTable.svelte
autonomous: true

must_haves:
  truths:
    - "Frontend sign-in flow works end-to-end: Clerk modal → authenticated requests → data displayed"
    - "Mutations from frontend include Bearer token and succeed for authenticated users"
    - "Unauthenticated users can view the Activity page (public queries)"
    - "Unauthenticated users see 'Sign In' button instead of UserButton"
    - "Add Video popover is only accessible to authenticated users"
    - "Existing user migration path is documented"
  artifacts:
    - path: "frontend/src/lib/components/AddVideoPopover.svelte"
      provides: "Add Video restricted to authenticated users"
      contains: "SignedIn"
  key_links: []
---

<objective>
Integration testing, existing user migration, and end-to-end verification of the complete Clerk authentication flow.

Purpose: Verify that all pieces work together — frontend auth, backend middleware, webhook sync, authorization directives, and ownership checks. Migrate existing users to Clerk. Handle edge cases and polish.

Output: Complete working authentication system, existing users migrated, edge cases handled.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-authentication/12-CONTEXT.md
@.planning/phases/12-authentication/12-01-PLAN.md
@.planning/phases/12-authentication/12-02-PLAN.md
@.planning/phases/12-authentication/12-03-PLAN.md
@.planning/phases/12-authentication/12-04-PLAN.md
@backend/cmd/server/main.go
@frontend/src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Gate mutation UI behind authentication</name>
  <files>
    frontend/src/lib/components/AddVideoPopover.svelte
  </files>
  <action>
    Wrap mutation triggers behind SignedIn/SignedOut:

    1. AddVideoPopover trigger button:
       - Only show "Add Video" button inside `<SignedIn>` block
       - Or show disabled button with tooltip "Sign in to add videos" for `<SignedOut>`

    2. Create User flow:
       - No longer needed as a frontend flow — Clerk handles user creation
       - Remove or disable CreateUserPopover if it exists

    3. Future perspective creation:
       - AddPerspectiveDialog (Phase 4) should also be gated by `<SignedIn>`
       - Document this for Phase 4 implementor

    ```svelte
    <script lang="ts">
      import { SignedIn, SignedOut } from 'svelte-clerk';
    </script>

    <SignedIn>
      <!-- existing AddVideoPopover trigger -->
    </SignedIn>
    <SignedOut>
      <button disabled class="opacity-50 cursor-not-allowed" title="Sign in to add videos">
        Add Video
      </button>
    </SignedOut>
    ```
  </action>
  <verify>
    grep -q "SignedIn" frontend/src/lib/components/AddVideoPopover.svelte || \
    grep -q "SignedIn" frontend/src/lib/components/Header.svelte
  </verify>
  <done>Mutation UI gated behind authentication</done>
</task>

<task type="auto">
  <name>Remove userId from mutation inputs</name>
  <files>
    frontend/src/lib/queries/
    backend/schema.graphql
    backend/internal/adapters/graphql/resolvers/
  </files>
  <action>
    With Clerk auth, the user is identified from the JWT — not from a frontend dropdown.

    1. Review GraphQL mutations that currently accept userId as input:
       - createPerspective: If input has userId field, remove it
       - createContentFromYouTube: If input has userId field, remove it
       - Backend resolvers now get user from auth.ForContext(ctx) instead of input

    2. Update schema.graphql input types if needed:
       - Remove userId from CreatePerspectiveInput (if present)
       - Remove userId from CreateContentFromYouTubeInput (if present)

    3. Update frontend mutation calls to stop sending userId.

    4. Run `make graphql-gen` after schema changes.

    Note: The UserSelector component was the source of userId. With Clerk, the
    backend determines the user from the JWT. Frontend never needs to specify userId.
  </action>
  <verify>
    make -C backend graphql-gen
    go build -C backend ./...
  </verify>
  <done>Mutations use authenticated user instead of input userId</done>
</task>

<task type="auto">
  <name>Document existing user migration</name>
  <files>
    backend/.env.example
  </files>
  <action>
    Document the migration procedure for existing users:

    1. **Migration script approach:**
       - Use Clerk Backend API to create users with matching emails
       - Store returned clerk_user_id in local users table
       - Users receive invitation to set password

       ```go
       // Example migration (run once):
       import clerkuser "github.com/clerk/clerk-sdk-go/v2/user"

       // For each real user (skip sentinel users):
       newClerkUser, err := clerkuser.Create(ctx, &clerkuser.CreateParams{
           EmailAddresses: &[]string{localUser.Email},
           Username:       clerk.String(localUser.Username),
       })
       // Update local record:
       UPDATE users SET clerk_user_id = $1 WHERE id = $2
       ```

    2. **Sentinel users:** `[deleted]` and `[system]` users remain local-only.
       They never authenticate and their clerk_user_id stays NULL.

    3. **Post-migration verification:**
       - All real users have clerk_user_id set
       - Clerk dashboard shows matching users
       - Users can sign in and access their content

    Add migration notes to .env.example or create a separate migration script file.
  </action>
  <verify>
    echo "Migration documentation task — no automated verification"
  </verify>
  <done>User migration procedure documented</done>
</task>

<task type="auto">
  <name>End-to-end verification</name>
  <files></files>
  <action>
    Verify the complete flow:

    1. **Backend build:** `cd backend && go build ./...`
    2. **Frontend build:** `cd frontend && pnpm run build`
    3. **Backend tests:** `cd backend && go test ./...`
    4. **Frontend tests:** `cd frontend && pnpm run test:run`

    5. **Manual E2E checklist (for developer):**
       - [ ] Start backend with CLERK_SECRET_KEY set
       - [ ] Start frontend with VITE_CLERK_PUBLISHABLE_KEY set
       - [ ] Activity page loads (unauthenticated, public query)
       - [ ] Click "Sign In" → Clerk modal appears
       - [ ] Sign in → UserButton appears in header
       - [ ] Add Video → Bearer token sent, video created
       - [ ] Sign out → UserButton replaced by "Sign In"
       - [ ] Try mutation while signed out → "authentication required" error

    6. **Webhook E2E checklist:**
       - [ ] Create user in Clerk dashboard → webhook fires → local user created
       - [ ] Update user in Clerk → local user updated
       - [ ] Delete user in Clerk → local user deactivated
  </action>
  <verify>
    cd backend && go build ./... && go test ./...
    cd frontend && pnpm run build
  </verify>
  <done>End-to-end authentication flow verified</done>
</task>

</tasks>

<verification>
1. Backend builds and tests pass
2. Frontend builds and tests pass
3. Mutation UI gated behind authentication
4. userId removed from mutation inputs
5. Existing user migration documented
6. E2E checklist provided for manual verification
</verification>

<success_criteria>
- [ ] Add Video button only visible to authenticated users
- [ ] userId removed from mutation inputs (backend uses JWT user)
- [ ] Schema regenerated after input changes
- [ ] Existing user migration procedure documented
- [ ] Sentinel users excluded from migration
- [ ] Backend builds with no errors
- [ ] Frontend builds with no errors
- [ ] Backend tests pass
- [ ] Frontend tests pass
- [ ] E2E verification checklist provided
</success_criteria>

<output>
After completion, create `.planning/phases/12-authentication/12-05-SUMMARY.md`
</output>
