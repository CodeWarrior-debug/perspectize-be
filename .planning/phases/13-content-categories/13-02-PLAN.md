---
phase: 13-content-categories
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/schema.graphql
  - backend/gqlgen.yml
  - backend/internal/core/domain/category.go
  - backend/internal/adapters/graphql/model/models_gen.go
  - backend/internal/adapters/graphql/resolvers/schema.resolvers.go
  - backend/internal/core/services/category_service.go
  - backend/cmd/server/main.go
autonomous: true

must_haves:
  truths:
    - "GraphQL schema defines Category type with id, name, slug, icon, contentCount fields"
    - "GraphQL schema defines ContentCategory enum with 20 UPPERCASE values bound to domain type"
    - "Content type includes optional category field of type Category"
    - "categories query returns list of all categories ordered by display_order"
    - "ContentFilter input includes category field of type ContentCategory"
    - "gqlgen.yml binds ContentCategory enum to domain.ContentCategory"
    - "CategoryService orchestrates category queries and content count"
    - "Resolver wires categories query to CategoryService"
    - "Content resolver includes category via dataloader or join"
    - "All backend tests pass after changes"
  artifacts:
    - path: "backend/schema.graphql"
      provides: "Category type, ContentCategory enum, categories query, category field on Content"
      contains: "ContentCategory"
    - path: "backend/gqlgen.yml"
      provides: "ContentCategory enum bound to domain.ContentCategory"
      contains: "ContentCategory"
    - path: "backend/internal/core/services/category_service.go"
      provides: "CategoryService with ListAll, GetByID methods"
      contains: "CategoryService"
    - path: "backend/internal/adapters/graphql/resolvers/schema.resolvers.go"
      provides: "categories query resolver, category field on content resolver"
      contains: "Categories"
  key_links:
    - from: "backend/schema.graphql"
      to: "backend/internal/adapters/graphql/model/models_gen.go"
      via: "gqlgen code generation"
      pattern: "ContentCategory"
    - from: "backend/internal/core/services/category_service.go"
      to: "backend/internal/core/ports/repositories/category_repository.go"
      via: "service depends on repository port"
      pattern: "CategoryRepository"
---

<objective>
Add GraphQL schema for categories, create CategoryService, and wire resolvers.

Purpose: After Plan 01 establishes the database and domain layers, this plan exposes categories through the GraphQL API. It adds the Category type, ContentCategory enum (bound via gqlgen), categories query, and category field on Content. The CategoryService handles business logic.

Output: Updated GraphQL schema with category support, CategoryService, resolvers, and main.go wiring.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-content-categories/13-CONTEXT.md
@.planning/v1.1-research/CONTENT-CATEGORIZATION.md (Section 4: GraphQL Integration)
@backend/CLAUDE.md (Enum & ID Handling section)

Key existing files to reference:
@backend/schema.graphql
@backend/gqlgen.yml
@backend/internal/core/services/ (existing service patterns)
@backend/internal/adapters/graphql/resolvers/schema.resolvers.go
@backend/cmd/server/main.go
</context>

<tasks>
1. Update `backend/schema.graphql`:
   - Add `ContentCategory` enum with 20 UPPERCASE values:
     ARTS_ENTERTAINMENT, AUTOS_VEHICLES, BEAUTY_FITNESS, BOOKS_LITERATURE,
     BUSINESS_INDUSTRIAL, COMPUTERS_ELECTRONICS, FINANCE, FOOD_DRINK,
     GAMES, HEALTH, HOBBIES_LEISURE, HOME_GARDEN, JOBS_EDUCATION,
     LAW_GOVERNMENT, NEWS, PEOPLE_SOCIETY, PETS_ANIMALS, SCIENCE,
     SPORTS, TRAVEL_TRANSPORTATION
   - Add `Category` type: id (IntID!), name (String!), slug (String!), icon (String), contentCount (Int!)
   - Add `category: Category` field to `Content` type
   - Add `categories: [Category!]!` query
   - Add `category: ContentCategory` to `ContentFilter` input (if exists from Phase 11) or create input

2. Update `backend/internal/core/domain/category.go`:
   - Add `ContentCategory` string type with 20 UPPERCASE constants
   - Add slug-to-enum and enum-to-slug converter functions

3. Update `backend/gqlgen.yml`:
   - Bind `ContentCategory` enum to `domain.ContentCategory`

4. Run `make graphql-gen` to regenerate Go types

5. Create `backend/internal/core/services/category_service.go`:
   - `CategoryService` struct with `categoryRepo ports.CategoryRepository`
   - `ListAll(ctx) ([]*domain.Category, error)` — returns all categories ordered
   - `GetByID(ctx, int) (*domain.Category, error)`
   - `GetContentCount(ctx, categoryID int) (int, error)` — count content with this category

6. Implement resolvers in `schema.resolvers.go`:
   - `categories` query → calls `CategoryService.ListAll()`
   - `Content.category` field resolver → loads category by content.CategoryID (use join or separate query)
   - Wire category filter in content list resolver if ContentFilter.Category is set

7. Wire CategoryService in `cmd/server/main.go`:
   - Instantiate GormCategoryRepository
   - Instantiate CategoryService
   - Pass to resolver

8. Run `go build ./...` and `go test ./...` in backend/ to verify
</tasks>
