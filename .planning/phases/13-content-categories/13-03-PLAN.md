---
phase: 13-content-categories
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/lib/queries/categories.ts
  - frontend/src/lib/components/CategoryPicker.svelte
  - frontend/src/lib/components/ActivityTable.svelte
  - frontend/src/lib/components/AddVideoPopover.svelte
autonomous: true

must_haves:
  truths:
    - "CategoryPicker component renders a Select dropdown with 20 categories including icons"
    - "CategoryPicker fetches categories via TanStack Query with 24-hour stale time"
    - "CategoryPicker supports bindable value prop for two-way binding"
    - "ActivityTable displays category column with category name and icon"
    - "ActivityTable category column is filterable via AG Grid set filter or header dropdown"
    - "AddVideoPopover includes optional CategoryPicker for assigning category on add"
    - "LIST_CATEGORIES GraphQL query defined in categories.ts"
    - "Query key factory includes categories namespace"
    - "All frontend tests pass after changes"
  artifacts:
    - path: "frontend/src/lib/queries/categories.ts"
      provides: "LIST_CATEGORIES query, queryKeys.categories factory, Category TypeScript type"
      contains: "LIST_CATEGORIES"
    - path: "frontend/src/lib/components/CategoryPicker.svelte"
      provides: "Reusable category dropdown with TanStack Query data fetching"
      contains: "CategoryPicker"
    - path: "frontend/src/lib/components/ActivityTable.svelte"
      provides: "Category column in AG Grid with name + icon display"
      contains: "category"
  key_links:
    - from: "frontend/src/lib/components/CategoryPicker.svelte"
      to: "frontend/src/lib/queries/categories.ts"
      via: "component uses LIST_CATEGORIES query"
      pattern: "categories"
    - from: "frontend/src/lib/components/ActivityTable.svelte"
      to: "frontend/src/lib/queries/content.ts"
      via: "content query now returns category field"
      pattern: "category"
---

<objective>
Build frontend CategoryPicker component and integrate categories into ActivityTable and AddVideoPopover.

Purpose: After Plans 01-02 establish the backend category support, this plan builds the user-facing components. Users can assign categories when adding videos, see categories in the activity table, and filter by category. The CategoryPicker is a reusable dropdown component.

Output: CategoryPicker component, category column in ActivityTable, category option in AddVideoPopover, and supporting query infrastructure.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-content-categories/13-CONTEXT.md
@.planning/v1.1-research/CONTENT-CATEGORIZATION.md (Section 5: Frontend UI Patterns)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/lib/queries/content.ts (query pattern reference)
@frontend/src/lib/components/ActivityTable.svelte
@frontend/src/lib/components/AddVideoPopover.svelte
@frontend/src/lib/components/shadcn/ (UI component library)
</context>

<tasks>
1. Create `frontend/src/lib/queries/categories.ts`:
   - Define `LIST_CATEGORIES` GraphQL query: `{ categories { id name slug icon contentCount } }`
   - Add `queryKeys.categories` factory: `{ list: () => ['categories', 'list'] }`
   - Export `Category` TypeScript interface matching GraphQL type

2. Create `frontend/src/lib/components/CategoryPicker.svelte`:
   - Accept `value` as bindable prop (number | null for category ID)
   - Fetch categories using `createQuery` with LIST_CATEGORIES
   - Use 24-hour stale time (categories rarely change)
   - Render shadcn Select component with category options
   - Each option shows icon + name (e.g., "🎮 Games")
   - Include "No category" option (null value)
   - Handle loading and error states

3. Update `frontend/src/lib/queries/content.ts`:
   - Add `category { id name slug icon }` to LIST_CONTENT query fields
   - Update Content TypeScript type to include optional category field

4. Update `frontend/src/lib/components/ActivityTable.svelte`:
   - Add category column to AG Grid column definitions
   - cellRenderer displays `icon + name` (e.g., "🎮 Games")
   - Column sortable and filterable
   - Position column after content_type or channel_title
   - Default width ~150px

5. Update `frontend/src/lib/components/AddVideoPopover.svelte`:
   - Add CategoryPicker below URL input (optional selection)
   - Pass selected categoryId to CREATE_CONTENT_FROM_YOUTUBE mutation
   - Pre-fill with auto-suggestion if available (future Plan 04)

6. Run `pnpm run test:run` in frontend/ to verify all tests pass
</tasks>
