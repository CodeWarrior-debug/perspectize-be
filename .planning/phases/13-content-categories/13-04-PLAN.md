---
phase: 13-content-categories
plan: 04
type: execute
wave: 4
depends_on: ["13-02", "13-03"]
files_modified:
  - backend/internal/adapters/ai/claude_client.go
  - backend/internal/adapters/ai/claude_client_test.go
  - backend/internal/core/ports/ai_service.go
  - backend/internal/core/services/content_service.go
  - backend/internal/adapters/graphql/resolvers/schema.resolvers.go
  - backend/schema.graphql
  - frontend/src/lib/components/AddVideoPopover.svelte
autonomous: true

must_haves:
  truths:
    - "Claude Haiku client sends title + description + tags to classify content into one of 20 categories"
    - "Claude client returns category slug and confidence score (0-1)"
    - "suggestCategory GraphQL query accepts contentId and returns CategorySuggestion with category, confidence, reasoning"
    - "AI port interface defines SuggestCategory method decoupled from Claude implementation"
    - "ContentService calls AI service on content creation to populate default category"
    - "Auto-suggestion only fires if content has no manually-set category"
    - "AddVideoPopover shows suggestion badge with confidence after video is added"
    - "Unit tests verify Claude client prompt construction and response parsing"
    - "Unit tests verify graceful handling of API errors and low confidence results"
    - "All backend and frontend tests pass"
  artifacts:
    - path: "backend/internal/adapters/ai/claude_client.go"
      provides: "ClaudeClient implementing AIService port with SuggestCategory using Haiku"
      contains: "SuggestCategory"
    - path: "backend/internal/adapters/ai/claude_client_test.go"
      provides: "Tests for prompt construction, response parsing, error handling"
      contains: "TestSuggestCategory"
    - path: "backend/internal/core/ports/ai_service.go"
      provides: "AIService interface with SuggestCategory method"
      contains: "AIService"
    - path: "backend/schema.graphql"
      provides: "suggestCategory query and CategorySuggestion type"
      contains: "CategorySuggestion"
  key_links:
    - from: "backend/internal/core/ports/ai_service.go"
      to: "backend/internal/adapters/ai/claude_client.go"
      via: "port interface implemented by Claude adapter"
      pattern: "AIService"
    - from: "backend/internal/core/services/content_service.go"
      to: "backend/internal/core/ports/ai_service.go"
      via: "service depends on AI port for category suggestion"
      pattern: "SuggestCategory"
---

<objective>
Add Claude Haiku auto-categorization with suggestion display in frontend.

Purpose: Manual category assignment (Plan 03) works but requires user effort. This plan adds Claude Haiku-powered auto-suggestion that classifies content based on title, description, and tags. Suggestions are presented to the user for confirmation, not auto-applied silently.

Output: AIService port, Claude Haiku client adapter, suggestCategory GraphQL query, auto-suggestion on content creation, and frontend suggestion display.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-content-categories/13-CONTEXT.md
@.planning/v1.1-research/CONTENT-CATEGORIZATION.md (Section 2.2: Claude API, Section 2.3: Hybrid Approach)
@backend/CLAUDE.md

Key existing files to reference:
@backend/internal/core/domain/category.go (from Plan 01)
@backend/internal/core/services/content_service.go
@backend/internal/adapters/graphql/resolvers/schema.resolvers.go
@frontend/src/lib/components/AddVideoPopover.svelte (from Plan 03)
</context>

<tasks>
1. Create `backend/internal/core/ports/ai_service.go`:
   - Define `CategorySuggestion` struct: CategorySlug string, Confidence float64, Reasoning string
   - Define `AIService` interface: `SuggestCategory(ctx, title string, description string, tags []string) (*CategorySuggestion, error)`

2. Create `backend/internal/adapters/ai/claude_client.go`:
   - `ClaudeClient` struct with Anthropic API client and category list
   - Implement `SuggestCategory`:
     - Build prompt with list of 20 category slugs and names
     - Send title + description + tags to Claude Haiku 3.5
     - Parse JSON response: `{"category_slug": "...", "confidence": 0.0-1.0, "reasoning": "..."}`
     - Return CategorySuggestion
   - Handle API errors gracefully (return nil suggestion, not error)
   - Handle low confidence (<0.5) by returning suggestion with low confidence flag

3. Update `backend/schema.graphql`:
   - Add `CategorySuggestion` type: category (Category!), confidence (Float!), reasoning (String)
   - Add `suggestCategory(contentId: IntID!): CategorySuggestion` query

4. Run `make graphql-gen`

5. Update `backend/internal/core/services/content_service.go`:
   - Add AIService dependency (optional — nil means auto-categorization disabled)
   - After content creation, if no category set and AIService available:
     - Call SuggestCategory with content title, description, tags
     - If confidence >= 0.7, auto-assign category (user can change later)
     - If confidence < 0.7, store suggestion but don't auto-assign

6. Implement `suggestCategory` resolver:
   - Load content by ID
   - Call AIService.SuggestCategory with content fields
   - Map category slug to Category object via CategoryRepository.GetBySlug
   - Return CategorySuggestion

7. Wire ClaudeClient in `cmd/server/main.go`:
   - Instantiate only if ANTHROPIC_API_KEY env var is set
   - Pass as optional AIService to ContentService

8. Write tests in `backend/internal/adapters/ai/claude_client_test.go`:
   - TestSuggestCategory_ConstructsCorrectPrompt (verify all 20 categories in prompt)
   - TestSuggestCategory_ParsesValidResponse
   - TestSuggestCategory_HandlesAPIError (returns nil, not error)
   - TestSuggestCategory_HandlesInvalidJSON (returns nil)
   - TestSuggestCategory_HandlesLowConfidence
   - Mock HTTP client for all tests (don't call real API)

9. Update `frontend/src/lib/components/AddVideoPopover.svelte`:
   - After successful video add, call suggestCategory query with new content ID
   - Display suggestion as: "Suggested: 🎮 Games (87% confident)" with Accept/Change buttons
   - Accept button calls updateContent mutation to set category
   - Change button opens CategoryPicker

10. Run `go test ./...` in backend/ and `pnpm run test:run` in frontend/
</tasks>
