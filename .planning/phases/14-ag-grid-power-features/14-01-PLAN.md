---
phase: 14-ag-grid-power-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/components/GridToolbar.svelte
  - frontend/src/lib/services/gridStateManager.ts
  - frontend/src/lib/components/ActivityTable.svelte
autonomous: true

must_haves:
  truths:
    - "GridToolbar component renders above ActivityTable with Clear Filters, Export CSV, and Reset buttons"
    - "gridStateManager service saves and loads filter state and column state to/from localStorage with perspectize: namespace"
    - "Clear Filters button calls gridApi.setFilterModel(null) and is only visible when filters are active"
    - "Reset button clears all localStorage state and restores default column/filter configuration"
    - "Filter and column state auto-saves to localStorage on change with 1-second debounce"
    - "State restores from localStorage on grid initialization (onGridReady)"
  artifacts:
    - path: "frontend/src/lib/components/GridToolbar.svelte"
      provides: "Toolbar component with Clear Filters, Export CSV, Reset buttons and reactive filter count badge"
      contains: "gridApi"
    - path: "frontend/src/lib/services/gridStateManager.ts"
      provides: "localStorage persistence service with save/load/clear for filter state, column state, and page size"
      contains: "perspectize:activityTable"
    - path: "frontend/src/lib/components/ActivityTable.svelte"
      provides: "Updated to include GridToolbar above grid, wire onGridReady to state restoration"
      contains: "GridToolbar"
  key_links:
    - from: "frontend/src/lib/services/gridStateManager.ts"
      to: "frontend/src/lib/components/GridToolbar.svelte"
      via: "import { saveFilterState, loadFilterState, saveColumnState, loadColumnState, clearAllState }"
      pattern: "saveFilterState"
    - from: "frontend/src/lib/components/GridToolbar.svelte"
      to: "frontend/src/lib/components/ActivityTable.svelte"
      via: "GridToolbar component receives gridApi prop"
      pattern: "GridToolbar"
---

<objective>
Create the GridToolbar component and localStorage state persistence service for the ActivityTable.

Purpose: Power users need one-click filter clearing, state persistence across sessions, and a reset-to-defaults option. Currently there is no toolbar, no state persistence, and no way to clear all filters at once.

Output: A GridToolbar.svelte component rendered above ActivityTable with Clear Filters, Export CSV, and Reset buttons. A gridStateManager.ts service that saves/loads grid state to/from localStorage with debounced auto-save.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ag-grid-power-features/14-CONTEXT.md
@.planning/v1.1-research/AG-GRID-POWER-FEATURES.md
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/lib/components/ActivityTable.svelte
@frontend/src/lib/components/shadcn/ui/button/index.ts
</context>

<tasks>
1. Create `frontend/src/lib/services/gridStateManager.ts`:
   - Define localStorage keys with `perspectize:activityTable:` namespace (filters, columns, pageSize)
   - Implement `saveFilterState(gridApi)` — gets filter model, serializes to localStorage
   - Implement `loadFilterState(gridApi)` — reads from localStorage, applies via `gridApi.setFilterModel()`
   - Implement `saveColumnState(gridApi)` — gets column state, serializes to localStorage
   - Implement `loadColumnState(gridApi)` — reads from localStorage, applies via `gridApi.applyColumnState()`
   - Implement `clearAllState()` — removes all perspectize localStorage keys
   - Implement `createDebouncedSaver(fn, delay=1000)` — returns debounced version of save function
   - Handle JSON parse errors gracefully (corrupted localStorage → silently clear and continue)
   - Export `DEFAULT_COLUMN_STATE` constant matching current ActivityTable columnDefs defaults

2. Create `frontend/src/lib/components/GridToolbar.svelte`:
   - Accept props: `gridApi` (AG Grid API instance)
   - Reactive state: `hasActiveFilters` ($state), `filterCount` ($state)
   - Update filter state on `onFilterChanged` callback from parent
   - Clear Filters button: calls `gridApi.setFilterModel(null)` + clears localStorage filter state
   - Show filter count badge next to Clear Filters when filters active (use shadcn Badge or inline span)
   - Export CSV button: calls `gridApi.exportDataAsCsv()` with timestamped filename
   - Reset button: calls `clearAllState()` + `gridApi.resetColumnState()` + `gridApi.setFilterModel(null)`
   - Layout: `flex items-center gap-2 px-2 py-1.5 bg-muted/50 rounded-t-lg border-b`
   - Use lucide-svelte icons: FilterX, Download, RotateCcw
   - All buttons use shadcn Button component with variant="ghost" size="sm"

3. Update `frontend/src/lib/components/ActivityTable.svelte`:
   - Import GridToolbar and gridStateManager
   - Add GridToolbar above AgGridSvelte, pass gridApi prop
   - In onGridReady: call `loadFilterState(gridApi)` and `loadColumnState(gridApi)`
   - Add onFilterChanged handler: update toolbar filter state + debounced save to localStorage
   - Add onColumnVisible/onColumnMoved/onSortChanged handlers: debounced save column state
   - Wrap grid + toolbar in `flex flex-col gap-0` container
</tasks>
