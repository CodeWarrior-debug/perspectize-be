---
phase: 14-ag-grid-power-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/components/GridToolbar.svelte
  - frontend/src/lib/components/ColumnVisibilityPicker.svelte
autonomous: true

must_haves:
  truths:
    - "Column visibility dropdown in GridToolbar shows all available columns with checkboxes"
    - "Toggling a checkbox immediately shows/hides the corresponding column in the grid"
    - "Column visibility state persists across sessions via localStorage (uses gridStateManager)"
    - "No infinite reactivity loops between Svelte 5 $effect and AG Grid callbacks"
    - "Hidden columns from default columnDefs remain hidden unless user explicitly shows them"
  artifacts:
    - path: "frontend/src/lib/components/ColumnVisibilityPicker.svelte"
      provides: "Dropdown menu with checkbox items for each grid column, synced bidirectionally with AG Grid"
      contains: "DropdownMenuCheckboxItem"
    - path: "frontend/src/lib/components/GridToolbar.svelte"
      provides: "Updated toolbar with Columns dropdown button between Clear Filters and Export CSV"
      contains: "ColumnVisibilityPicker"
  key_links:
    - from: "frontend/src/lib/components/ColumnVisibilityPicker.svelte"
      to: "frontend/src/lib/components/GridToolbar.svelte"
      via: "imported and rendered inside toolbar"
      pattern: "ColumnVisibilityPicker"
---

<objective>
Add a column visibility picker dropdown to the GridToolbar, allowing users to show/hide columns without developer tools.

Purpose: Users currently have no way to control which columns are visible. Some columns (like Tags, Description) may be irrelevant to certain users. A dropdown with checkboxes per column gives users control over their view.

Output: A ColumnVisibilityPicker.svelte component rendered inside GridToolbar as a Columns dropdown button. Bidirectional sync between checkbox state and AG Grid column visibility, with localStorage persistence.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ag-grid-power-features/14-CONTEXT.md
@.planning/v1.1-research/AG-GRID-POWER-FEATURES.md (Section 2: Toolbar Component Design, Section 6: Svelte 5 Integration Patterns)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/lib/components/ActivityTable.svelte
@frontend/src/lib/components/GridToolbar.svelte (created in Plan 01)
@frontend/src/lib/services/gridStateManager.ts (created in Plan 01)
@frontend/src/lib/components/shadcn/ui/dropdown-menu/
</context>

<tasks>
1. Create `frontend/src/lib/components/ColumnVisibilityPicker.svelte`:
   - Accept props: `gridApi` (AG Grid API instance)
   - On mount / when gridApi available: read column definitions to build checkbox list
   - For each column: show `DropdownMenuCheckboxItem` with `headerName` label and `checked` = `!col.hide`
   - On checkbox change: call `gridApi.setColumnsVisible([colId], checked)` for the specific column
   - Use tracking variable (`isUpdatingFromState`) to prevent infinite loops:
     - When programmatically setting visibility → set flag → skip grid callback
     - When grid fires onColumnVisible → check flag → update local state only if not programmatic
   - Exclude non-user-facing columns from picker (e.g., row index if any)
   - Use shadcn DropdownMenu components: DropdownMenuTrigger, DropdownMenuContent, DropdownMenuCheckboxItem, DropdownMenuLabel, DropdownMenuSeparator
   - Trigger button: shadcn Button variant="ghost" size="sm" with Columns icon (lucide-svelte)

2. Update `frontend/src/lib/components/GridToolbar.svelte`:
   - Import ColumnVisibilityPicker
   - Add between Clear Filters and Export CSV buttons
   - Add visual separator (div with border-r) between filter controls and column controls
   - Pass gridApi to ColumnVisibilityPicker
   - Ensure column visibility changes trigger the debounced column state save (already wired in Plan 01's onColumnVisible handler)
</tasks>
