---
phase: 14-ag-grid-power-features
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - frontend/src/lib/components/GridToolbar.svelte
  - frontend/src/lib/components/ActivityTable.svelte
  - frontend/src/lib/services/gridStateManager.ts
  - frontend/src/tests/gridStateManager.test.ts
  - frontend/src/tests/GridToolbar.test.ts
autonomous: true

must_haves:
  truths:
    - "CSV export downloads a file named perspectize-content-YYYY-MM-DD.csv with visible columns only"
    - "Keyboard shortcut Ctrl+Shift+F clears all filters"
    - "Keyboard shortcut Ctrl+Shift+E exports CSV"
    - "Keyboard shortcut Ctrl+Shift+R resets to defaults"
    - "Keyboard shortcuts only fire when ActivityTable page is active (cleanup on unmount)"
    - "gridStateManager unit tests cover save, load, clear, and corrupted localStorage recovery"
    - "GridToolbar renders correctly with all buttons and responds to filter state changes"
    - "Multi-column sort enabled via multiSortKey: 'ctrl' in grid options"
    - "All existing frontend tests continue to pass"
  artifacts:
    - path: "frontend/src/tests/gridStateManager.test.ts"
      provides: "Unit tests for localStorage persistence: save/load filter state, save/load column state, clear all, corrupted data handling"
      contains: "perspectize:activityTable"
    - path: "frontend/src/tests/GridToolbar.test.ts"
      provides: "Component tests for GridToolbar rendering, button clicks, filter count badge visibility"
      contains: "GridToolbar"
    - path: "frontend/src/lib/components/GridToolbar.svelte"
      provides: "Updated with CSV export sanitization (formula injection prevention) and keyboard shortcut display"
      contains: "exportDataAsCsv"
    - path: "frontend/src/lib/components/ActivityTable.svelte"
      provides: "Updated with multiSortKey, keyboard shortcut registration/cleanup"
      contains: "multiSortKey"
  key_links:
    - from: "frontend/src/tests/gridStateManager.test.ts"
      to: "frontend/src/lib/services/gridStateManager.ts"
      via: "imports and tests all exported functions"
      pattern: "saveFilterState"
---

<objective>
Add CSV export with security sanitization, keyboard shortcuts, multi-column sort, and comprehensive tests for the AG Grid power features.

Purpose: Complete the toolbar feature set with export capability, keyboard shortcuts for power users, and ensure reliability through tests. CSV export needs formula injection prevention. Keyboard shortcuts provide fast access for frequent operations.

Output: Working CSV export with sanitized output, keyboard shortcuts (Ctrl+Shift+F/E/R), multi-column sort via Ctrl+click, and test coverage for gridStateManager and GridToolbar.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ag-grid-power-features/14-CONTEXT.md
@.planning/v1.1-research/AG-GRID-POWER-FEATURES.md (Section 4: Keyboard Shortcuts, Section 5: Export Functionality)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/lib/components/GridToolbar.svelte (from Plan 01 + 02)
@frontend/src/lib/components/ActivityTable.svelte (from Plan 01)
@frontend/src/lib/services/gridStateManager.ts (from Plan 01)
@frontend/vitest.config.ts
@frontend/src/tests/ (existing test patterns)
</context>

<tasks>
1. Update CSV export in GridToolbar:
   - Export CSV button calls `gridApi.exportDataAsCsv()` with:
     - `fileName: 'perspectize-content-${new Date().toISOString().split('T')[0]}.csv'`
     - `allColumns: false` (export only visible columns)
     - `processCellCallback`: sanitize values starting with `+`, `-`, `=`, `@` (CSV injection prevention — prepend with single quote)
   - Show toast notification on export: `toast.success('CSV exported')`

2. Add keyboard shortcuts to ActivityTable:
   - Register window keydown listener in `$effect` (Svelte 5 pattern with cleanup)
   - Ctrl+Shift+F → clear all filters (same as toolbar button)
   - Ctrl+Shift+E → export CSV (same as toolbar button)
   - Ctrl+Shift+R → reset to defaults (same as toolbar button)
   - `e.preventDefault()` on all matched shortcuts to avoid browser defaults
   - Cleanup: remove event listener on component unmount (return cleanup from $effect)
   - Add `kbd` elements to toolbar button tooltips showing shortcuts

3. Enable multi-column sort in ActivityTable:
   - Add `multiSortKey: 'ctrl'` to gridOptions (hold Ctrl+click headers for multi-sort)
   - This is a one-line config change — AG Grid Community supports it natively

4. Write unit tests for gridStateManager (`frontend/src/tests/gridStateManager.test.ts`):
   - Test `saveFilterState` writes to correct localStorage key
   - Test `loadFilterState` reads and applies filter model
   - Test `saveColumnState` / `loadColumnState` round-trip
   - Test `clearAllState` removes all perspectize keys
   - Test corrupted localStorage (invalid JSON) → gracefully clears and continues
   - Test debounced saver only fires after delay
   - Mock localStorage and gridApi for all tests

5. Write component tests for GridToolbar (`frontend/src/tests/GridToolbar.test.ts`):
   - Test renders all buttons (Clear Filters, Columns, Export CSV, Reset)
   - Test Clear Filters button hidden when no filters active
   - Test filter count badge shows correct number
   - Test button click handlers are called
   - Use @testing-library/svelte for component rendering

6. Run `pnpm run test:run` in frontend/ to verify all tests pass
</tasks>
