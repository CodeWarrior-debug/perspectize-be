---
phase: 15-discover-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/discover/+page.svelte
  - frontend/src/routes/discover/+page.ts
  - frontend/src/lib/services/youtubeApi.ts
  - frontend/src/lib/components/discover/SearchBar.svelte
  - frontend/src/lib/components/discover/VideoCard.svelte
  - frontend/src/lib/components/discover/VideoResultsGrid.svelte
  - frontend/src/lib/components/Header.svelte
autonomous: true

must_haves:
  truths:
    - "/discover route renders with search input and results area"
    - "SearchBar debounces input at 300ms before triggering search, placeholder text is \"Search Content Sources...\""
    - "youtubeApi.ts calls YouTube Data API v3 search.list with VITE_YOUTUBE_API_KEY"
    - "youtubeApi.ts calls YouTube Data API v3 videos.list?chart=mostPopular with VITE_YOUTUBE_API_KEY via fetchYouTubeTrending(regionCode = 'US')"
    - "youtubeKeys includes a trending() query key alongside search(), with 1-hour staleTime for the trending query"
    - "When no search query is active, page shows Trending results with a \"Showing Trending Content\" label above the grid instead of a category grid"
    - "VideoResultsGrid and VideoCard are shape-agnostic: a small adapter normalizes search.list and videos.list snippet shapes into one common video-item type consumed by both components"
    - "Search results display thumbnail, title, channel, publish date, and description"
    - "Add to Library button calls existing useAddVideo mutation with constructed YouTube URL"
    - "Already-added videos show disabled button with checkmark"
    - "Load More button fetches next page using nextPageToken"
    - "TanStack Query caches search results with 5-minute stale time"
    - "Header navigation includes Discover link alongside Activity"
    - "Loading and error states handle API responses gracefully"
    - "Page holds an internal view state ('search' | 'trending') with no visible tab/toggle UI yet — structured so a future Browse mode can be added without reworking the layout"
    - "All frontend tests pass"
  artifacts:
    - path: "frontend/src/lib/services/youtubeApi.ts"
      provides: "fetchYouTubeSearch and fetchYouTubeTrending functions, SearchParams/SearchResponse/TrendingResponse types, youtubeKeys query key factory (search + trending)"
      contains: "fetchYouTubeTrending"
    - path: "frontend/src/routes/discover/+page.svelte"
      provides: "Discover page with search input, trending default view, results list, and load more pagination"
      contains: "SearchBar"
    - path: "frontend/src/lib/components/discover/VideoCard.svelte"
      provides: "Video card with thumbnail, metadata, and Add to Library button, reusable for search and trending items"
      contains: "Add"
    - path: "frontend/src/lib/components/discover/VideoResultsGrid.svelte"
      provides: "Reusable results grid (list + load more + loading/empty/error) driven by a normalized video-item array"
      contains: "VideoCard"
    - path: "frontend/src/lib/components/Header.svelte"
      provides: "Updated navigation with Discover link"
      contains: "discover"
  key_links:
    - from: "frontend/src/routes/discover/+page.svelte"
      to: "frontend/src/lib/services/youtubeApi.ts"
      via: "page uses TanStack Query with youtubeApi fetch functions (search and trending)"
      pattern: "fetchYouTubeSearch"
    - from: "frontend/src/lib/components/discover/VideoCard.svelte"
      to: "frontend/src/lib/queries/content.ts"
      via: "useAddVideo mutation for one-click add"
      pattern: "useAddVideo"
---

<objective>
Create the Discover page with YouTube search, a Trending default view, result display, and one-click add to library.

Purpose: Users currently must find videos on YouTube externally, copy URLs, and paste them into Perspectize. This plan creates a /discover route with in-app YouTube search, reducing the add flow from 4+ clicks to 2 (search + click add). Before a query is entered, the page shows YouTube's Trending chart rather than an empty state. This is the MVP search experience — category Browse is deferred (see 15-03, skipped) but the page is structured with an internal view state so Browse can be added later without a rework.

Output: /discover route with SearchBar (debounced, placeholder "Search Content Sources..."), a Trending default view ("Showing Trending Content" label + grid), VideoResultsGrid with thumbnails and metadata, VideoCard with Add to Library button, Load More pagination, and Header navigation update.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-discover-page/15-CONTEXT.md
@.planning/v1.1-research/DISCOVER-PAGE.md (Section 2: YouTube API, Section 4: Search UX, Section 6: Content Preview & Add)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/routes/+page.svelte (existing home page pattern)
@frontend/src/lib/components/Header.svelte
@frontend/src/lib/components/AddVideoPopover.svelte (existing useAddVideo pattern)
@frontend/src/lib/queries/content.ts (query key patterns)
</context>

<tasks>
1. Create `frontend/src/lib/services/youtubeApi.ts`:
   - Define TypeScript interfaces: SearchParams, SearchResponse, SearchResultItem, TrendingResponse
   - Define a common `VideoItem` type that both search and trending results are normalized into (id, title, channelTitle, publishedAt, description, thumbnails) — write a small adapter function per source (`toVideoItem(searchResultItem)`, `toVideoItem(trendingItem)`) so downstream components never see the raw API shape
   - Implement `fetchYouTubeSearch(params: SearchParams): Promise<SearchResponse>`
     - Builds URL: `https://www.googleapis.com/youtube/v3/search`
     - Sets: `part=snippet`, `type=video`, `key=VITE_YOUTUBE_API_KEY`
     - Maps params: q, maxResults (25), pageToken, order
     - Throws on non-OK response with status code
   - Implement `fetchYouTubeTrending(regionCode: string = 'US', pageToken?: string): Promise<TrendingResponse>`
     - Builds URL: `https://www.googleapis.com/youtube/v3/videos`
     - Sets: `part=snippet,contentDetails`, `chart=mostPopular`, `regionCode`, `key=VITE_YOUTUBE_API_KEY`
     - Throws on non-OK response with status code
   - Export query key factory:
     ```typescript
     export const youtubeKeys = {
       all: ['youtube'] as const,
       searches: () => [...youtubeKeys.all, 'search'] as const,
       search: (query: string, filters?: SearchFilters) =>
         [...youtubeKeys.searches(), query, filters] as const,
       trending: (regionCode: string = 'US') =>
         [...youtubeKeys.all, 'trending', regionCode] as const,
     };
     ```

2. Create `frontend/src/routes/discover/+page.ts`:
   - Export `export const prerender = false;` (dynamic API calls)

3. Create `frontend/src/lib/components/discover/SearchBar.svelte`:
   - Text input with placeholder "Search Content Sources..."
   - Debounce logic (300ms) using $effect or setTimeout
   - Expose `debouncedQuery` as bindable prop
   - Clear button (X icon) to reset search
   - Autofocus on mount

4. Create `frontend/src/lib/components/discover/VideoCard.svelte`:
   - Props: video (VideoItem, the normalized type), isInLibrary (boolean), onAdd callback
   - Layout: thumbnail (320x180) | title + channel + date + description | Add button
   - Add button: calls onAdd(videoId)
   - Disabled state when isInLibrary (show "✓ In Library")
   - Pending state during mutation ("Adding...")
   - Use shadcn Card, Button components
   - Named generically (not SearchResultCard) since it renders both search and trending items identically

5. Create `frontend/src/lib/components/discover/VideoResultsGrid.svelte`:
   - Props: items (VideoItem[]), nextPageToken, onLoadMore, libraryUrls (Set<string>), label (optional string, e.g. "Showing Trending Content")
   - Renders optional label above the grid, then a list of VideoCard components
   - Load More button at bottom when nextPageToken exists
   - Empty state: "No results found" when items empty
   - Loading state with skeleton cards
   - Named generically (not SearchResults) since it renders both search and trending result sets

6. Create `frontend/src/routes/discover/+page.svelte`:
   - State: searchQuery, debouncedQuery, pageToken, allResults (accumulated)
   - Internal view state: `let view = $derived<'search' | 'trending'>(debouncedQuery ? 'search' : 'trending')` — no visible toggle/tab UI yet, this is purely a code seam so a future Browse mode can extend the union and slot in without restructuring the page
   - TanStack Query for search:
     - queryKey: youtubeKeys.search(debouncedQuery)
     - staleTime: 5 minutes
     - enabled: view === 'search'
   - TanStack Query for trending:
     - queryKey: youtubeKeys.trending()
     - staleTime: 1 hour
     - enabled: view === 'trending'
   - TanStack Query for library content (to check already-added):
     - Use existing LIST_CONTENT query to get all URLs
     - Build Set<string> of library URLs for O(1) lookup
   - Wire useAddVideo mutation from existing code
   - Normalize whichever query's raw response is active into VideoItem[] via the adapters from youtubeApi.ts before passing to VideoResultsGrid
   - Pass label="Showing Trending Content" to VideoResultsGrid only when view === 'trending'
   - Accumulate results across pages (append on Load More)
   - Reset accumulated results when search query or view changes

7. Update `frontend/src/lib/components/Header.svelte`:
   - Add "Discover" navigation link pointing to /discover
   - Position before or after existing "Activity" link
   - Highlight active route

8. Run `pnpm run test:run` in frontend/ to verify
</tasks>
