---
phase: 15-discover-page
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/lib/components/discover/FilterBar.svelte
  - frontend/src/lib/services/youtubeApi.ts
  - frontend/src/routes/discover/+page.svelte
autonomous: true

must_haves:
  truths:
    - "FilterBar renders Duration, Upload Date, and Sort Order dropdown selects"
    - "Duration filter maps to YouTube videoDuration parameter: short (<4min), medium (4-20min), long (>20min)"
    - "Upload Date filter maps to publishedAfter parameter with relative date calculation"
    - "Sort Order filter maps to YouTube order parameter: relevance, date, viewCount, rating"
    - "Changing any filter triggers automatic refetch via TanStack Query key change"
    - "Clear Filters button resets all filters to defaults and is visible only when filters are active"
    - "Filter state included in youtubeKeys.search() query key"
    - "All frontend tests pass"
  artifacts:
    - path: "frontend/src/lib/components/discover/FilterBar.svelte"
      provides: "Duration, Upload Date, and Sort Order filter dropdowns with clear button"
      contains: "videoDuration"
    - path: "frontend/src/lib/services/youtubeApi.ts"
      provides: "Updated SearchParams with duration, publishedAfter, order fields"
      contains: "videoDuration"
    - path: "frontend/src/routes/discover/+page.svelte"
      provides: "Filter state wired to TanStack Query key for auto-refetch"
      contains: "FilterBar"
  key_links:
    - from: "frontend/src/lib/components/discover/FilterBar.svelte"
      to: "frontend/src/routes/discover/+page.svelte"
      via: "filter state props with bindable values"
      pattern: "filters"
    - from: "frontend/src/routes/discover/+page.svelte"
      to: "frontend/src/lib/services/youtubeApi.ts"
      via: "filters passed to fetchYouTubeSearch and included in query key"
      pattern: "youtubeKeys.search"
---

<objective>
Add search filters (duration, upload date, sort order) to the Discover page.

Purpose: Basic search (Plan 01) returns results sorted by relevance only. This plan adds filter controls that map to YouTube API parameters, letting users narrow results by video duration, upload date, and sort order. Filters auto-trigger refetch via TanStack Query key changes.

Output: FilterBar component with three dropdowns, updated API service with filter parameters, and auto-refetch integration.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-discover-page/15-CONTEXT.md
@.planning/v1.1-research/DISCOVER-PAGE.md (Section 4.3: Filters)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/routes/discover/+page.svelte (from Plan 01)
@frontend/src/lib/services/youtubeApi.ts (from Plan 01)
@frontend/src/lib/components/shadcn/ (Select component)
</context>

<tasks>
1. Update `frontend/src/lib/services/youtubeApi.ts`:
   - Extend SearchParams interface with optional fields:
     - `videoDuration?: 'short' | 'medium' | 'long'`
     - `publishedAfter?: string` (ISO 8601)
     - `order?: 'relevance' | 'date' | 'viewCount' | 'rating'`
   - Update fetchYouTubeSearch to set URL params when present
   - Export `SearchFilters` type for filter state

2. Create `frontend/src/lib/components/discover/FilterBar.svelte`:
   - Props: `filters` as bindable (SearchFilters type)
   - Duration select:
     - Options: Any duration, Under 4 minutes (short), 4-20 minutes (medium), Over 20 minutes (long)
   - Upload Date select:
     - Options: Any time, Last hour, Today, This week, This month, This year
     - Calculate publishedAfter ISO string from relative option
   - Sort Order select:
     - Options: Relevance, Upload date, View count, Rating
   - Clear Filters button:
     - Only visible when any filter differs from defaults
     - Resets all to: duration=undefined, date=undefined, order='relevance'
   - Use shadcn Select components for consistent styling

3. Update `frontend/src/routes/discover/+page.svelte`:
   - Add filters state: `let filters = $state({ duration: undefined, publishedAfter: undefined, order: 'relevance' })`
   - Include filters in TanStack Query key: `youtubeKeys.search(debouncedQuery, filters)`
   - Pass filters to fetchYouTubeSearch
   - Reset page token and accumulated results when filters change
   - Render FilterBar between SearchBar and SearchResults

4. Run `pnpm run test:run` in frontend/ to verify
</tasks>
