---
phase: 15-discover-page
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - frontend/src/lib/components/discover/CategoryGrid.svelte
  - frontend/src/lib/components/discover/CategoryCard.svelte
  - frontend/src/lib/services/youtubeApi.ts
  - frontend/src/routes/discover/+page.svelte
autonomous: true

must_haves:
  truths:
    - "CategoryGrid renders YouTube video categories as clickable cards in a responsive grid"
    - "Categories fetched from YouTube videoCategories.list API with 24-hour TanStack Query cache"
    - "Each CategoryCard shows Lucide icon and category name"
    - "Clicking a category card triggers search filtered by videoCategoryId"
    - "CategoryGrid shown when no search query is active (default Discover view)"
    - "Breadcrumb navigation allows returning from category view to all categories"
    - "youtubeApi.ts includes fetchYouTubeCategories function and videoCategoryId in SearchParams"
    - "All frontend tests pass"
  artifacts:
    - path: "frontend/src/lib/components/discover/CategoryGrid.svelte"
      provides: "Responsive grid of YouTube category cards with TanStack Query data fetching"
      contains: "CategoryGrid"
    - path: "frontend/src/lib/components/discover/CategoryCard.svelte"
      provides: "Individual category card with icon and name, clickable"
      contains: "CategoryCard"
    - path: "frontend/src/lib/services/youtubeApi.ts"
      provides: "fetchYouTubeCategories function and videoCategoryId parameter support"
      contains: "fetchYouTubeCategories"
  key_links:
    - from: "frontend/src/lib/components/discover/CategoryGrid.svelte"
      to: "frontend/src/lib/services/youtubeApi.ts"
      via: "fetches categories via youtubeApi, triggers category-filtered search on click"
      pattern: "fetchYouTubeCategories"
    - from: "frontend/src/routes/discover/+page.svelte"
      to: "frontend/src/lib/components/discover/CategoryGrid.svelte"
      via: "conditionally renders CategoryGrid when no search query"
      pattern: "CategoryGrid"
---

<objective>
Add YouTube category browsing with clickable category cards and category-filtered search.

Purpose: Not all discovery starts with a text query. Category browsing lets users explore trending content by topic (Music, Gaming, Sports, etc.). This plan adds a category grid as the default Discover view, with click-to-search functionality.

Output: CategoryGrid and CategoryCard components, fetchYouTubeCategories API function, category-filtered search, and conditional rendering in Discover page.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-discover-page/15-CONTEXT.md
@.planning/v1.1-research/DISCOVER-PAGE.md (Section 5: Category Browsing)
@frontend/CLAUDE.md

Key existing files to reference:
@frontend/src/routes/discover/+page.svelte (from Plan 01)
@frontend/src/lib/services/youtubeApi.ts (from Plan 01)
</context>

<tasks>
1. Update `frontend/src/lib/services/youtubeApi.ts`:
   - Add `VideoCategoriesResponse` and `VideoCategory` TypeScript interfaces
   - Implement `fetchYouTubeCategories(regionCode: string = 'US'): Promise<VideoCategory[]>`
     - Calls `https://www.googleapis.com/youtube/v3/videoCategories`
     - Params: `part=snippet`, `regionCode`, `key=VITE_YOUTUBE_API_KEY`
     - Filters to only `assignable: true` categories
   - Add `videoCategoryId?: string` to SearchParams
   - Update fetchYouTubeSearch to set videoCategoryId param when present
   - Add to youtubeKeys: `categories: () => [...youtubeKeys.all, 'categories'] as const`

2. Create icon mapping utility (in youtubeApi.ts or separate file):
   - Map YouTube category IDs to Lucide icon names:
     - 1 (Film & Animation) → Film
     - 2 (Autos & Vehicles) → Car
     - 10 (Music) → Music
     - 15 (Pets & Animals) → PawPrint
     - 17 (Sports) → Trophy
     - 19 (Travel & Events) → Plane
     - 20 (Gaming) → Gamepad2
     - 22 (People & Blogs) → Users
     - 23 (Comedy) → Laugh
     - 24 (Entertainment) → Tv
     - 25 (News & Politics) → Newspaper
     - 26 (Howto & Style) → Wrench
     - 27 (Education) → GraduationCap
     - 28 (Science & Technology) → FlaskConical
     - Default → Tag

3. Create `frontend/src/lib/components/discover/CategoryCard.svelte`:
   - Props: category (VideoCategory), icon (Lucide component), onClick callback
   - Render: card with icon + category name
   - Hover effect for interactivity
   - Use shadcn Card variant

4. Create `frontend/src/lib/components/discover/CategoryGrid.svelte`:
   - Fetch categories via TanStack Query with 24-hour staleTime
   - Render responsive CSS grid (3 cols desktop, 2 tablet, 1 mobile)
   - Map each category to CategoryCard with icon
   - Props: onSelect callback (passes categoryId)

5. Update `frontend/src/routes/discover/+page.svelte`:
   - Add selectedCategory state: `let selectedCategory = $state<string | null>(null)`
   - Conditional rendering:
     - No search query AND no category → show CategoryGrid
     - Search query OR category selected → show SearchResults
   - When category selected, pass videoCategoryId to search query
   - Add breadcrumb: "All Categories > Music" when category active
   - Clicking breadcrumb "All Categories" clears selectedCategory

6. Run `pnpm run test:run` in frontend/ to verify
</tasks>
