---
phase: 16-mobile-app-strategy
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md
autonomous: false

must_haves:
  truths:
    - "App loads in iOS Simulator showing the Activity table"
    - "Navigation works within the WebView (no white screen on route change)"
    - "GraphQL API calls succeed from native WebView context"
    - "AG Grid table renders and scrolls without performance issues"
    - "PWA manifest is served correctly on web (installable prompt available)"
    - "Recommendation document captures POC findings and next steps"
  artifacts:
    - path: ".planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md"
      provides: "Final recommendation with POC findings, decision matrix, and next steps"
      contains: "Recommendation"
  key_links:
    - from: "frontend/capacitor.config.ts"
      to: "iOS Simulator"
      via: "cap open ios"
      pattern: "Xcode build and run"
    - from: ".planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md"
      to: ".planning/phases/16-mobile-app-strategy/16-RESEARCH.md"
      via: "Research findings validated by POC"
---

<objective>
Verify the Capacitor POC works in iOS Simulator and document the recommendation with findings.

Purpose: Validate the research conclusions with a working proof-of-concept before committing to the mobile strategy. The checkpoint lets the user see the app in Simulator and confirm it works.
Output: Verified POC in iOS Simulator + recommendation document with findings and next steps.
</objective>

<execution_context>
@/Users/jamesjordan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesjordan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mobile-app-strategy/16-RESEARCH.md
@.planning/phases/16-mobile-app-strategy/16-01-SUMMARY.md
@frontend/capacitor.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Capacitor POC in iOS Simulator</name>
  <what-built>
    Capacitor integration with the existing SvelteKit app. The app should load in iOS Simulator via Xcode, showing the Activity table with all existing functionality.
  </what-built>
  <how-to-verify>
    **Pre-flight (Claude runs these before checkpoint):**
    ```bash
    cd frontend
    pnpm run build
    npx cap sync
    npx cap open ios
    ```
    This opens Xcode with the Perspectize project.

    **User verification steps:**

    1. **Build and Run in Xcode:**
       - In Xcode, select an iPhone simulator (e.g., iPhone 16)
       - Press Cmd+R or click the Play button to build and run
       - Wait for the app to launch in Simulator

    2. **Verify app loads:**
       - [ ] App launches without white screen
       - [ ] Navy header (#1a365d) is visible at top
       - [ ] Activity table (AG Grid) renders with data rows
       - [ ] "Add Video" button is visible in header

    3. **Verify navigation:**
       - [ ] Tap on table rows — no crashes or white screens
       - [ ] Scroll the AG Grid table — smooth performance
       - [ ] Pagination controls work (next/prev page)

    4. **Verify GraphQL:**
       - [ ] Data loads in the Activity table (proves GraphQL API calls work from WebView)
       - [ ] If no data appears, check if the backend is running and accessible from the Simulator's network

    5. **Verify AG Grid performance:**
       - [ ] Table scrolling is smooth (not janky or laggy)
       - [ ] Column sorting works (tap column headers)
       - [ ] Filters work (if floating filters are visible)

    6. **Note any issues:**
       - Record any visual glitches, performance problems, or broken functionality
       - Check Xcode console for errors/warnings

    **PWA verification (separate — on web):**
    7. Run `cd frontend && pnpm run preview` and open in Chrome
       - [ ] Open Chrome DevTools > Application > Manifest
       - [ ] Verify manifest shows "Perspectize" with navy theme color
       - [ ] Check for installable prompt (may need HTTPS for full test)

    **If iOS Simulator is not available** (no Xcode installed):
    - Skip iOS verification steps 1-6
    - Test Android instead: `npx cap open android` (requires Android Studio)
    - Or test web build only: `pnpm run preview` and verify app works
    - Note in resume signal what was/wasn't testable
  </how-to-verify>
  <resume-signal>
    Type "approved" with any notes about issues found, or describe problems to fix.
    Include: which platform was tested (iOS Simulator, Android Emulator, or web-only), any performance observations, any visual issues.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write recommendation document with POC findings</name>
  <files>
    .planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md
  </files>
  <action>
    Create `.planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md` with the following structure. Incorporate actual POC results from the checkpoint (what worked, what didn't, any issues found):

    ```markdown
    # Phase 16: Mobile App Strategy — Recommendation

    **Date:** 2026-02-16
    **Status:** POC Complete
    **Recommendation:** Capacitor for store distribution + PWA for web installability

    ## Executive Summary

    [1-2 paragraphs summarizing the recommendation, based on research + POC validation]

    ## POC Results

    ### What Was Tested
    - Platform: [iOS Simulator / Android Emulator / Web]
    - Capacitor version: [version installed]
    - Build output size: [size of frontend/build/]

    ### Results
    | Criterion | Result | Notes |
    |-----------|--------|-------|
    | App loads in Simulator | [PASS/FAIL] | [details] |
    | Navigation works | [PASS/FAIL] | [details] |
    | GraphQL API calls | [PASS/FAIL] | [details] |
    | AG Grid performance | [PASS/FAIL] | [details] |
    | PWA manifest served | [PASS/FAIL] | [details] |

    ### Issues Found
    [List any issues discovered during POC, with severity and workaround if applicable]

    ## Decision Matrix

    | Approach | Store Distribution | Dev Effort | Update Speed | Native APIs | Recommendation |
    |----------|-------------------|------------|--------------|-------------|----------------|
    | Capacitor | YES | Medium (1-2 days setup) | OTA for JS, store for native | Full plugin ecosystem | PRIMARY |
    | PWA | NO (web only) | Low (2-4 hours) | Instant deploy | Limited (WebAuthn, Web Push) | COMPLEMENT |
    | Tauri Mobile | YES (risky) | High (DX issues) | Similar to Capacitor | Immature ecosystem | DO NOT USE |

    ## Architecture

    ### Production Setup (when ready to ship)
    - Capacitor 7.x with iOS + Android platforms
    - @vite-pwa/sveltekit for web installability
    - @capacitor/preferences for auth token storage (replaces localStorage on native)
    - @capacitor/haptics for App Store compliance
    - bundleStrategy: 'single' if WebView load time exceeds 2s

    ### Files Added
    - `frontend/capacitor.config.ts` — Capacitor configuration
    - `frontend/ios/` — Xcode project (checked into git)
    - `frontend/android/` — Android Studio project (checked into git)
    - `frontend/src/lib/utils/native.ts` — Platform detection utilities

    ## Prerequisites Before Shipping

    1. **Phase 12 (Authentication)** must be complete — auth flows need Capacitor-specific handling
    2. **Apple Developer Account** ($99/year) for App Store distribution
    3. **Google Play Developer Account** ($25 one-time) for Play Store distribution
    4. **Real app icons** — replace placeholder icons with designed assets (192px, 512px, 512px maskable)
    5. **@capacitor/preferences** — replace localStorage with native storage for auth tokens
    6. **CORS configuration** — verify backend accepts `capacitor://localhost` and `http://localhost` origins
    7. **Privacy policy** — required for both App Store and Play Store submission

    ## Next Steps (When Phase 12 is Complete)

    1. Integrate Clerk auth SDK with Capacitor WebView
    2. Replace localStorage auth storage with @capacitor/preferences
    3. Add native haptic feedback to key interaction points
    4. Create production app icons
    5. Test CORS with native WebView origins
    6. Submit to TestFlight (iOS) and internal testing track (Android)
    7. Consider bundleStrategy: 'single' based on load time testing
    8. Evaluate @capgo/capacitor-updater for OTA updates

    ## Open Questions Resolved

    [Fill in based on POC findings — which open questions from RESEARCH.md were answered]
    ```

    **Adapt the template based on actual POC results.** If the checkpoint revealed issues, document them honestly. If certain features weren't testable (e.g., no Xcode available), note that clearly.
  </action>
  <verify>
    1. `ls .planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md` exists
    2. File contains "POC Results" section with actual test findings
    3. File contains "Decision Matrix" with all three approaches compared
    4. File contains "Prerequisites Before Shipping" section
    5. File contains "Next Steps" section
  </verify>
  <done>
    Recommendation document captures POC findings, decision matrix, prerequisites for production, and concrete next steps. The document serves as the authoritative reference for mobile strategy decisions.
  </done>
</task>

</tasks>

<verification>
1. POC verification checkpoint completed — user confirmed app loads (or documented blockers)
2. `ls .planning/phases/16-mobile-app-strategy/16-RECOMMENDATION.md` — recommendation exists
3. Recommendation includes actual POC test results (not just theoretical)
4. All research open questions addressed with POC findings
</verification>

<success_criteria>
- User has seen the app running in iOS Simulator (or documented why Simulator wasn't available)
- Recommendation document created with POC-validated findings
- Clear next steps documented for when Phase 12 (auth) is complete
- Phase 16 goal achieved: research completed, recommendation produced, POC validated
</success_criteria>

<output>
After completion, create `.planning/phases/16-mobile-app-strategy/16-02-SUMMARY.md`
</output>
