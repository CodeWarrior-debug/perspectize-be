---
phase: 18-server-side-pagination-filtering-with-data-mode-toggle
plan: "01"
subsystem: api
tags: [graphql, gqlgen, gorm, postgres, jsonb, pagination, filtering, sorting]

# Dependency graph
requires:
  - phase: 07.2-gorm-cursor-paginator
    provides: gorm-cursor-paginator sort rules pattern (buildContentSortRules, dummy GORM fields)
  - phase: 03.2-activity-page-beta-quality
    provides: ContentFilter GraphQL input and GORM filtering patterns
provides:
  - Expanded ContentFilter with 13 new filter fields (view/like counts, dates, channel, tags, description)
  - New ContentSortBy enum values CHANNEL_TITLE and LENGTH
  - Full JSONB-extraction WHERE clauses in List() for all new filters
  - Sort rules for CHANNEL_TITLE (JSONB) and LENGTH (direct column) in buildContentSortRules
  - ChannelTitle dummy GORM field for paginator key validation
  - Resolver mapping of all new filter fields to domain struct
affects: [18-02, 18-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - JSONB extraction filter pattern: WHERE clause using response->'items'->0->'statistics'->>'viewCount' with ::BIGINT cast
    - ISO 8601 string comparison for date filters on JSONB fields (publishedAt)
    - JSONB array text cast for tag search: (tags)::text ILIKE '%term%'

key-files:
  created: []
  modified:
    - backend/schema.graphql
    - backend/internal/core/domain/pagination.go
    - backend/internal/adapters/repositories/postgres/gorm_content_repository.go
    - backend/internal/adapters/repositories/postgres/helpers.go
    - backend/internal/adapters/repositories/postgres/gorm_models.go
    - backend/internal/adapters/graphql/resolvers/schema.resolvers.go
    - backend/internal/adapters/graphql/model/models_gen.go
    - backend/internal/adapters/graphql/generated/generated.go

key-decisions:
  - "JSONB filter performance: O(n) scans acceptable for <1000 items; Phase 11 will add indexes"
  - "Tag search uses (tags)::text ILIKE for MVP; can upgrade to jsonb_array_elements_text subquery later"
  - "ISO 8601 string comparison valid for publishedAt date filtering (lexicographic order matches chronological)"

patterns-established:
  - "JSONB extraction filter: WHERE \"response->'items'->0->...'->>'field'\" ILIKE or ::BIGINT comparison"
  - "Dummy GORM field pattern extended: ChannelTitle added alongside ViewCount, LikeCount, PublishedAt"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-02-28
---

# Phase 18 Plan 01: Backend Expanded Filtering & Sorting Summary

**Expanded ContentFilter to 17 fields and ContentSortBy to 8 values with full JSONB-extraction WHERE clauses and sort rules for server-side filtering on all AG Grid columns**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-28T00:31:13Z
- **Completed:** 2026-02-28T00:35:44Z
- **Tasks:** 6
- **Files modified:** 8

## Accomplishments

- Added 13 new fields to ContentFilter input in schema.graphql, domain struct, models_gen.go, and resolver mapping
- Added CHANNEL_TITLE and LENGTH to ContentSortBy enum with full sort rule implementations
- Repository List() method now applies JSONB-extraction WHERE clauses for view counts, like counts, published date, channel title, tags, description, and record timestamps
- All existing backend tests pass (go build and go test both succeed)

## Task Commits

Each task was committed atomically:

1. **Tasks 1-3 (pre-existing): Schema, Domain, Repository** - `4f9cd6c` + `335e97d` (feat - already committed prior to this execution)
2. **Task 4: Sort Rules + ChannelTitle dummy field** - `b387609` (feat)
3. **gqlgen regeneration: models_gen.go + generated.go** - `499c3b5` (chore)
4. **Task 5: Resolver filter mapping** - `84e4c44` (feat)

## Files Created/Modified

- `backend/schema.graphql` - Added 13 new ContentFilter fields and CHANNEL_TITLE/LENGTH to ContentSortBy enum
- `backend/internal/core/domain/pagination.go` - Expanded ContentFilter struct and added ContentSortByChannelTitle/ContentSortByLength constants
- `backend/internal/adapters/repositories/postgres/gorm_content_repository.go` - Added WHERE clauses for all 13 new filter fields using JSONB extraction
- `backend/internal/adapters/repositories/postgres/helpers.go` - Added CHANNEL_TITLE and LENGTH cases to buildContentSortRules switch
- `backend/internal/adapters/repositories/postgres/gorm_models.go` - Added ChannelTitle dummy field for paginator key validation
- `backend/internal/adapters/graphql/resolvers/schema.resolvers.go` - Mapped all 13 new ContentFilter fields from GraphQL model to domain struct
- `backend/internal/adapters/graphql/model/models_gen.go` - Auto-regenerated by gqlgen with expanded ContentFilter struct
- `backend/internal/adapters/graphql/generated/generated.go` - Auto-regenerated by gqlgen with new schema bindings

## Decisions Made

- JSONB filter performance is O(n) without indexes — acceptable for current scale (<1000 items). Phase 11 (Database Optimization) will add indexes.
- Tag search uses `(tags)::text ILIKE` which casts entire JSONB array to text. Simple and correct for MVP; can upgrade to `jsonb_array_elements_text` subquery or `@>` operator later.
- ISO 8601 date strings can be compared lexicographically for publishedAt since they sort correctly as strings.

## Deviations from Plan

None - plan executed exactly as written. Tasks 1, 2, and 3 were discovered to be pre-completed (schema, domain struct, and repository WHERE clauses were already committed). Tasks 4, 5, and 6 were completed in this execution.

## Issues Encountered

None. The gqlgen code generation (go run github.com/99designs/gqlgen generate) was required to regenerate models_gen.go and generated.go after the schema changes — this was expected per the plan's Task 6 step.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Backend filtering/sorting fully expanded and ready for frontend consumption in Plan 18-02 (Data Mode Toggle frontend changes)
- All new ContentFilter fields available via GraphQL for frontend AG Grid column filters
- New sort options CHANNEL_TITLE and LENGTH available for frontend sort dropdown

## Self-Check: PASSED

- All 6 modified files exist on disk
- All 3 execution commits found (b387609, 499c3b5, 84e4c44)
- go build ./... exits 0 with no errors
- go test ./... passes all 13 test packages

---
*Phase: 18-server-side-pagination-filtering-with-data-mode-toggle*
*Completed: 2026-02-28*
