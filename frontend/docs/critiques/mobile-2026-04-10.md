# Mobile Views Critique — 2026-04-10

> Generated by `/critique mobile views`. Review against the design brief in `.impeccable.md`.
> Score: **20/40** — needs meaningful work. Target range for shipped products: 28–34.
> Scope: mobile viewports (xs <445px, sm 445–639px). Desktop issues are out of scope.

---

## TL;DR

The **engineering** is considerably better than the **mobile design**. The architecture honors the brief (dual Geist/Charter fonts, AG Grid tier system, FormPopover mobile→Dialog swap, pagination over infinite scroll, rating tokens in `app.css`). But on a 375×812 phone the user sees a generic admin dashboard for a YouTube database, not a calm editorial reading room.

**The single biggest opportunity:** the mobile home is ~100% chrome and ~0% perspective content. Kill the wrapping card, hide the "Activity" H1 on xs/sm, let the list bleed edge-to-edge, and rewrite every system string in the project's brief voice. Those two moves alone would jump the heuristic score ~4–6 points and eliminate the "admin panel" first-impression.

**Open question not yet answered:** should the mobile home *be* the AG Grid at all, or should mobile become a card-list? Deferred for a later session — see Question 2 below.

---

## Anti-Patterns Verdict

### What's absent (good)
- No gradient text
- No `border-left` accent stripes
- No glassmorphism
- No infinite scroll
- No avatar feeds / social chrome
- No red YouTube-clone accents

### What's creeping in (bad)
- **Card-in-card nesting.** `+page.svelte:52` wraps the AG Grid in `border rounded-lg shadow-sm`, and `ActivityTable.svelte:652–683` puts the FilterChips band *inside* that card. The brief explicitly bans this.
- **Hardcoded saturation colors outside tokens.** `PerspectivePopover.svelte:227–247` uses `bg-green-500` / `bg-red-500` / `border-green-500` / `border-red-500`. These bypass `--color-rating-positive` / `--color-rating-negative` (defined `app.css:82–84`) and will render as neon artifacts in every future curated theme.
- **Bland system strings.** "Activity" / "Recently updated content" / "No content yet" / "Failed to load content. Please try again." / "Loading…" — every one is LLM-default corporate. The brief's single biggest voice lever is un-pulled.
- **Hardcoded AG Grid hexes.** `ActivityTable.svelte:242–257` sets `headerBackgroundColor: '#1a365d'` and other raw hexes in `themeQuartz.withParams()`, bypassing theme tokens.

### Deterministic scan (npx impeccable)
One finding:
- **`pure-black-white`** in `frontend/src/lib/components/shadcn/dialog/dialog-overlay.svelte:12` — uses `bg-black` for the dialog scrim. Shadcn-svelte default. Replace with `bg-foreground/80` or a tinted `oklch(12% 0.01 250)`. Note: `PerspectivePopover.svelte:174` has the *opposite* failure — `overlay=bg-transparent`. The project is making inconsistent scrim choices.

---

## Design Health Score

| #   | Heuristic                          | Score    | Key Issue |
|-----|------------------------------------|----------|-----------|
| 1   | Visibility of System Status        | 3/4      | Loading/pending/error states wired; toasts global. "Loaded N Items" vs "All Items" mode only explained via desktop hover tooltip that doesn't fire on touch. |
| 2   | Match System / Real World          | 2/4      | "Activity" / "Recently updated content" / "All Items" is system-speak. A newcomer on mobile sees a data grid, not a perspectives app. |
| 3   | User Control and Freedom           | 2/4      | No undo after submitting a perspective, no draft-save in PerspectivePopover, RatingInput clear-X is `-right-5` absolute and can clip offscreen. |
| 4   | Consistency and Standards          | 2/4      | Two Add-Video components in parallel. PerspectivePopover rolls its own Dialog with transparent overlay. No single "sheet vs dialog vs popover" policy. |
| 5   | Error Prevention                   | 2/4      | No confirmation on "Clear all filters". RatingInput uses `step="any"`. Touch targets on stepper chevrons (`size-3.5`) and clear-X (`size-3`) fail 44×44. |
| 6   | Recognition Rather Than Recall     | 2/4      | Filter chips are a win, but AG Grid column-filter menus on touch are near-unreachable. On mobile, chips can only be *cleared*, never *created*. |
| 7   | Flexibility and Efficiency         | 1/4      | Mobile amputation is the headline failure. At xs the grid shows only Perspectize/Item/Type. Page-size desktop-only. Next/Prev collapse to unlabeled `<`/`>`. No gestures, no share-target. |
| 8   | Aesthetic and Minimalist Design    | 2/4      | Five horizontal chrome bands on an 812px phone over ~2–3 rows of data. Where the brief is strongest in theory, weakest in practice. |
| 9   | Error Recovery                     | 3/4      | Retry button, form validation, toasts. Copy is competent but corporate — not the "warm, dry, literary" voice the brief demands. |
| 10  | Help and Documentation             | 1/4      | Effectively none on mobile. PerspectivePopover info tooltip is `opacity-0 group-hover:opacity-100` — unreachable on touch. RatingInput 2×2 grid has no explanation of what "Agreement" or "Confidence" mean. |
| **Total** |                              | **20/40**| **Needs meaningful work** |

---

## What's Working

1. **AG Grid responsive tier system** — `ActivityTable.svelte:567–629`. Four tiers, explicit column-reveal order, correctly uses `matchMedia` with `isMobile: true` requirement. Genuinely thoughtful engineering and the right bone structure for any future "calm mobile" work.

2. **`FormPopover`'s mobile→Dialog auto-swap** — `FormPopover.svelte:50–88`. Correctly detects `(max-width: 767px)` and swaps popover for dialog. Add-Video and Create-User benefit automatically. This is the right pattern — it just needs to become the project's *only* dialog pattern. PerspectivePopover currently rolls its own and gets it wrong.

3. **`RatingInput`'s numerals-as-jewelry execution** — `RatingInput.svelte:138–216`. Tabular monospace numerals, click-to-set progress bar, value-driven color via rating tokens (not hex), clear button on interaction. At the component level this is the brief's "numerals as jewelry" promise being kept.

4. **Charter is actually loaded and used in body cells** — `app.css:152–154`. Not flattened to Geist. The dual-font signature survives.

---

## Priority Issues

### [P1] Mobile home has no hero and no voice — it's a table in a card
**Files:** `+page.svelte:29–56`, `Header.svelte:6`, `ActivityTable.svelte:652`.

**Why it matters:** The brief says "Content is sovereign" and "chrome recedes." On a 375×812 phone the user sees 5 horizontal chrome bands (header, page-header, filter band, grid header, pagination footer) over ~2–3 rows of data. That is the opposite of the intent, and it's the #1 reason the first impression reads as generic SaaS.

**Fix:**
- Kill the `border rounded-lg shadow-sm` card wrapper on mobile (`+page.svelte:52`). Let the list bleed edge-to-edge.
- Reduce page-header padding on xs/sm (`px-4 md:px-6` → `px-0 sm:px-6`, or hide entirely on xs).
- Replace the "Activity" H1 on mobile with a Charter editorial moment — a teaser of the most recently saved perspective, or a single literary line. Or drop the H1 entirely on xs.
- Decide (see Question 2 below): mobile = list vs mobile = grid-with-fixes.

**Suggested commands:** `/arrange` for layout restructure, then `/distill` to rip out chrome that isn't earning its keep.

---

### [P1] Touch targets below 44×44 across rating + filter + pagination controls
**Files:**
- `RatingInput.svelte:147–199` — stepper chevrons `size-3.5`, clear-X `size-3` absolute `-right-5`
- `FilterChips.svelte:147–153` — X inside `p-0.5`
- `ActivityTable.svelte:714–730` — pagination `<`/`>` at `px-3 py-1 text-sm`

**Why it matters:** WCAG 2.5.5 and Apple HIG both want 44pt minimum. The `RatingInput` clear-X positioned `-right-5` is especially risky at 320px viewports — inside a 2-col grid with `gap-4` in a `max-w-sm` dialog with `px-6+px-2` padding, it can clip offscreen entirely.

**Fix:**
- Stepper buttons: `p-2 min-w-[44px] min-h-[44px]`.
- Clear-X: move inline next to the number, not absolutely positioned.
- Pagination: replace `<`/`>` with full "Prev"/"Next" labels, `min-h-[44px]`.
- `FilterChips` X: expand hit area to ≥32px without growing the visible X glyph.

**Suggested command:** `/harden` for the touch-target pass.

---

### [P1] PerspectivePopover hover-only tooltip + hardcoded saturation colors
**Files:**
- `PerspectivePopover.svelte:183–197` — `opacity-0 group-hover:opacity-100` info tooltip
- `PerspectivePopover.svelte:227–247` — `bg-green-500` / `bg-red-500` / `border-green-500` / `border-red-500`
- `PerspectivePopover.svelte:174` — `overlay=bg-transparent`

**Why it matters:**
- (a) The help tooltip explaining "Add as much or as little as you like" is unreachable on touch. Mobile users will never see it. That's the Help-and-Documentation 1/4 score in one widget.
- (b) The saturated greens/reds bypass `--color-rating-positive` / `--color-rating-negative`. Every future curated theme (Archive, Garden, Midnight, Terminal) will render them as neon artifacts.
- (c) The transparent overlay makes the dialog float over the grid with no scrim — reads as a floating panel, not a modal focus moment.

**Fix:**
- Replace group-hover tooltip with a small always-visible Charter-italic subtitle beneath the perspective name.
- Replace `bg-green-500` / `bg-red-500` with `bg-[var(--color-rating-positive)]` / `bg-[var(--color-rating-negative)]` or their Tailwind-token equivalents.
- Give the dialog a real scrim (`bg-foreground/80` or tinted variant).

**Suggested commands:** `/normalize` to pull colors back onto tokens, then `/clarify` for the tooltip rewrite.

---

### [P2] Every system string is LLM-default corporate — the biggest voice lever is un-pulled
**Files:** `ActivityTable.svelte:526, 562–563, 660` · `+page.svelte:35` · `+layout.svelte:45` · `AddVideoDialog.svelte:74` · most toasts.

**Why it matters:** The brief says verbatim: "Empty states, 404s, loading states, and system messages are the places to be playful… Humor and personality appear in copy, in microcopy, in empty states, in 404s, in the way a number is set." Every one of these strings currently reads like a generic admin template. Zero editorial voice, zero cleverness, zero New-Yorker-caption-writer energy.

**Fix:** Rewrite every system string in-voice. Illustrative directions (not final copy):

| Location | Before | Toward |
|----------|--------|--------|
| Empty state | "No content yet" | "Nothing in the archive yet. Paste a video to begin." |
| No results | "No results match your filters" | "Nothing here matches. Loosen the filters, or just scroll." |
| Load error | "Failed to load content. Please try again." | "The archive is quiet. Try again in a moment." |
| Page subtitle | "Recently updated content" | Charter-italic literary phrase — "What you've been thinking about" or similar |
| Loading | "Loading..." | "Gathering your perspectives…" or a Charter-italic ellipsis |

Don't commit these verbatim — the actual copy needs the voice tuned intentionally. But the pattern is clear: every string gets written, no string is default.

**Suggested command:** `/clarify` with the explicit mandate to rewrite system strings in the `.impeccable.md` voice.

---

### [P2] Orphaned UserSelector + Clerk inconsistency (latent bug)
**Files:**
- `Header.svelte:15–24` — uses Clerk's `SignInButton` / `UserButton`
- `UserSelector.svelte` — 75 lines, not rendered anywhere
- `CreateUserPopover.svelte` — referenced only by UserSelector
- `ActivityTable.svelte:115` — reads `getSelectedUserId()` from `userSelection.svelte`

**Why it matters:** The entire perspective system depends on a `selectedUserId` that no mounted UI sets on mobile. Clerk is user source-of-truth in Header, but ActivityTable reads a separate store. This is simultaneously a data-flow bug and a design inconsistency.

**Fix:** Pick one:
1. Delete `UserSelector.svelte` + `CreateUserPopover.svelte`. Wire Clerk's `useUser()` id into `selectedUserId` via a small `$effect` in `+layout.svelte`. (Cleaner, matches "Clerk is source of truth".)
2. Or keep UserSelector and mount it inside Header's `<Show when="signed-in">` block.

**Suggested command:** `/distill` with a specific mandate to delete the orphaned user-switcher.

---

## Persona Red Flags

### Jordan (First-Timer) — general consumer, opens on phone after watching a video
- Opens the app. Sees navy header + "Perspectize" wordmark + "Add Video" button + search box + "Activity" + bordered table. **"Is this a YouTube admin panel?"** Abandons before reading a single perspective.
- If they tap "Add Video": has to leave the app to copy URL from YouTube, come back, paste, submit. No share-target intent. 4–5 app switches for one entry — "effortless to input" is violated.
- Taps PerspectivePopover's quality slider, wonders what "Agreement" means, gets zero help (tooltip hover-only). Abandons mid-form.

### Alex (Power User / Curator) — the heavy-user audience the brief names
- Wants efficiency on mobile: sort by date, jump to a specific perspective, clear filters quickly. AG Grid filter menus are near-unreachable on touch, so filter chips can only be *cleared* never *created*.
- Tries to page through the list. Unlabeled `<`/`>` buttons. Squints. `DataModeToggle` "Loaded N / All Items" distinction has no explanation on mobile.
- Opens PerspectivePopover to write a detailed review. RatingInput clear-X clips offscreen at 375px. Can't correct misinput without re-stepping. Writes half a review, gives up, switches to desktop.

### Mira (thoughtful curator, first custom-theme attempt) — project-specific
- Discovers Perspectize has no theme picker at all on mobile (the brief says it's first-class). Disappointed.
- Once a theme system exists: her "Archive" theme renders PerspectivePopover's `bg-green-500` / `bg-red-500` as jarring neon in a sepia reading room.

---

## Minor Observations

- [ ] `+page.svelte:29` — `h-[calc(100vh-4rem)]` fights iOS Safari dynamic toolbar. Use `100svh` or `100dvh`.
- [ ] `UserSelector.svelte:58–60` — `w-28` too narrow for most usernames (also unused; see P2).
- [ ] `ActivityTable.svelte:242–257` — hardcoded `headerBackgroundColor: '#1a365d'` and other hexes in AG Grid `themeQuartz.withParams()`. Bypasses theme tokens.
- [ ] `AddVideoDialog.svelte:74`, `AddVideoPopover.svelte:76` — `text-red-600` instead of `text-destructive`.
- [ ] `app.css:91–149` — tooltip styles use rgba hex backgrounds. Theme-breaking.
- [ ] PWA is wired (`+layout.svelte:10–11, 27–37`) but no "Add to Home Screen" nudge on mobile.
- [ ] No theme picker exists at all. Brief calls it first-class; codebase has no theme store, no picker route, no affordance.
- [ ] `shadcn/dialog/dialog-overlay.svelte:12` — `bg-black` scrim (detector finding). Use `bg-foreground/80`.

---

## Questions to Consider

1. **What if the mobile home *is* a single perspective, not a grid?** The brief says "the perspective is the hero object, the table is just a way to find it." On mobile, why is the table the first thing the user sees? A Charter-set cover card with today's most recent perspective, followed by a light recent-list, would honor the brief in a way the current layout can't.

2. **Is AG Grid even the right tool on mobile?** At xs/sm you've amputated it to 3 columns. That's not a grid, it's a list with extra steps. A native Svelte card-list (same URL state, sort, filter) would feel more like a reading room — and unlock swipe affordances, tap-to-expand, and proper touch targets by default. **Open — deferred for a later session.**

3. **Where is the theme picker?** The brief calls it first-class. Currently: not started. If the first thing a mobile user can customize is their reading room, that's a peak-end moment waiting to happen. Could this be where you start reclaiming the "beautiful, clever" axis?

4. **What does "effortless to input" actually mean on a phone?** Right now adding a video is: tap Add → leave to YouTube → copy → come back → paste → submit. Should the mobile version be a share-target intent (Android share sheet + iOS Shortcut), so YouTube → Share → Perspectize is one tap with auto-populated URL?

5. **Would removing the H1 "Activity" break anything?** The word is content-free and corporate. If you deleted it on mobile, what would you put in its place? If the answer is "nothing, just the list," the chrome was never earning its keep.

---

## When You Come Back: Action Checklist

Work order recommended by impact. Check off as you go. Re-run `/critique mobile views` after fixes to measure score movement.

### Layout & hero (biggest first-impression delta)
- [ ] **`/arrange`** — Restructure mobile home. Kill the `border rounded-lg shadow-sm` card wrapper on `+page.svelte:52`. Reduce or drop page-header padding on xs/sm. Let the list bleed edge-to-edge.
- [ ] **`/distill`** — Drop/rework the "Activity" H1 + "Recently updated content" subtitle on mobile. Replace with a Charter editorial moment or nothing.

### Touch targets & theme-breakers
- [ ] **`/harden`** — 44×44 pass on RatingInput steppers & clear-X, FilterChips close button, pagination Prev/Next. Move the RatingInput clear-X inline. Replace `<`/`>` with "Prev"/"Next".
- [ ] **`/normalize`** — Pull PerspectivePopover colors onto rating tokens (`bg-green-500`/`bg-red-500` → `var(--color-rating-positive/negative)`). Fix AG Grid hardcoded hexes in `ActivityTable.svelte:242–257`. Fix `text-red-600` → `text-destructive` in AddVideo components. Fix the dialog overlay scrim inconsistency.

### Voice & microcopy
- [ ] **`/clarify`** — Rewrite every system string (empty, no-results, load error, page subtitle, loading, toasts) in the `.impeccable.md` voice. Replace PerspectivePopover hover-only tooltip with always-visible Charter-italic subtitle. *Review copy before shipping — don't commit the illustrative strings in this doc verbatim.*

### Architecture cleanup (low urgency but removes a latent bug)
- [ ] **`/distill`** — Decide UserSelector fate. Delete orphaned `UserSelector.svelte` + `CreateUserPopover.svelte`, wire Clerk `useUser()` id into `selectedUserId` in `+layout.svelte`.

### Minor sweeps
- [ ] iOS Safari viewport: `100vh` → `100svh` on `+page.svelte:29`
- [ ] Tooltip styles in `app.css:91–149` — pull onto tokens
- [ ] Consider a PWA "Add to Home Screen" nudge for the mobile install flow

### Open decisions (not ready yet)
- [ ] **Mobile shape:** AG Grid card-list vs keep-and-fix. Revisit when above is done. See Question 2.
- [ ] **Theme picker:** scope out as its own phase. See Question 3.
- [ ] **Share-target intent:** scope out as its own phase. See Question 4.

### Final pass
- [ ] **`/polish`** — After any subset of the above is done, run `/polish` to catch lingering details.
- [ ] **`/critique mobile views`** — Re-run to measure score movement. Target: 28+ out of 40.

---

*Generated by `/critique mobile views` on 2026-04-10. Design brief lives at `.impeccable.md`.*
